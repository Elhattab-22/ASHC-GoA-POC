import { n as __toESM } from "./rolldown-runtime-FDOR9p9I.js";
import { t as require_react } from "./react.js";
import { t as require_jsx_runtime } from "./react_jsx-runtime.js";
//#region node_modules/@abgov/react-components/index.mjs
var import_jsx_runtime = require_jsx_runtime();
var import_react = /* @__PURE__ */ __toESM(require_react(), 1);
var __defProp = Object.defineProperty;
var __typeError = (msg) => {
	throw TypeError(msg);
};
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, {
	enumerable: true,
	configurable: true,
	writable: true,
	value
}) : obj[key] = value;
var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);
var __accessCheck = (obj, member, msg) => member.has(obj) || __typeError("Cannot " + msg);
var __privateAdd = (obj, member, value) => member.has(obj) ? __typeError("Cannot add the same private member more than once") : member instanceof WeakSet ? member.add(obj) : member.set(obj, value);
var __privateMethod = (obj, member, method) => (__accessCheck(obj, member, "access private method"), method);
var _PublicFormController_instances;
var dispatchError_fn;
var lowercase = (input) => input.toLowerCase();
var kebab = (input) => input.replace(/([a-z])([A-Z])/g, "$1-$2").toLowerCase();
function transformProps(props, transform = lowercase) {
	const result = {};
	for (const [key, value] of Object.entries(props)) {
		if (value === void 0) continue;
		if (key.startsWith("data-")) result[key] = value;
		else result[transform(key)] = value;
	}
	return result;
}
function GoabAccordion({ open, onChange, headingContent, headingType, actions, children, ...rest }) {
	const ref = (0, import_react.useRef)(null);
	const _props = transformProps(rest, lowercase);
	(0, import_react.useEffect)(() => {
		const element = ref.current;
		if (element && onChange) {
			const handler = (event) => {
				onChange?.(event.detail.open);
			};
			element.addEventListener("_change", handler);
			return () => {
				element.removeEventListener("_change", handler);
			};
		}
	}, [onChange]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("goa-accordion", {
		ref,
		open: open ? "true" : void 0,
		"heading-type": headingType,
		..._props,
		children: [
			headingContent && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				slot: "headingcontent",
				children: headingContent
			}),
			actions && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				slot: "actions",
				children: actions
			}),
			children
		]
	});
}
function GoabAppHeader({ onMenuClick, children, secondaryText, ...rest }) {
	const el = (0, import_react.useRef)(null);
	const _props = transformProps(rest, lowercase);
	(0, import_react.useEffect)(() => {
		if (!el.current) return;
		if (!onMenuClick) return;
		const current = el.current;
		const listener = () => {
			onMenuClick?.();
		};
		current.addEventListener("_menuClick", listener);
		return () => {
			current.removeEventListener("_menuClick", listener);
		};
	}, [el, onMenuClick]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-app-header", {
		ref: el,
		hasmenuclickhandler: onMenuClick ? "true" : "false",
		secondarytext: secondaryText,
		..._props,
		version: "2",
		children
	});
}
function GoabAppHeaderMenu({ children, slotName, ...rest }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-app-header-menu", {
		slot: slotName,
		...transformProps(rest, lowercase),
		children
	});
}
function getIconValue(icon, iconType) {
	if (icon !== void 0) return icon ? "true" : "false";
	return iconType ? "true" : "false";
}
function GoabBadge({ icon, iconType, size = "medium", emphasis = "strong", ...rest }) {
	const _props = transformProps({
		size,
		emphasis,
		...rest
	}, lowercase);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-badge", {
		icon: getIconValue(icon, iconType),
		icontype: iconType,
		..._props,
		version: "2",
		size,
		emphasis
	});
}
function GoabBlock({ testId, children, ...rest }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-block", {
		...transformProps({
			testid: testId,
			...rest
		}, kebab),
		children
	});
}
function GoabButton({ disabled, onClick, actionArgs, actionArg, children, ...rest }) {
	const el = (0, import_react.useRef)(null);
	const _props = transformProps(rest, lowercase);
	(0, import_react.useEffect)(() => {
		if (!el.current) return;
		if (!onClick) return;
		const current = el.current;
		const listener = () => {
			onClick?.();
		};
		current.addEventListener("_click", listener);
		return () => {
			current.removeEventListener("_click", listener);
		};
	}, [el, onClick]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-button", {
		ref: el,
		disabled: disabled ? "true" : void 0,
		"action-arg": actionArg,
		"action-args": JSON.stringify(actionArgs),
		..._props,
		version: "2",
		children
	});
}
function GoabButtonGroup({ children, ...rest }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-button-group", {
		...transformProps(rest, lowercase),
		children
	});
}
function GoabCalendar({ min, max, onChange, name, ...rest }) {
	const ref = (0, import_react.useRef)(null);
	const _props = transformProps(rest, lowercase);
	(0, import_react.useEffect)(() => {
		if (!ref.current) return;
		const current = ref.current;
		const listener = (e) => {
			onChange({
				name: name || "",
				value: e.detail.value
			});
		};
		current.addEventListener("_change", listener);
		return () => {
			current.removeEventListener("_change", listener);
		};
	}, []);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-calendar", {
		ref,
		name,
		min: min || void 0,
		max: max || void 0,
		version: "2",
		..._props
	});
}
var GoabCallout = ({ type = "information", iconTheme = "outline", size = "large", ariaLive = "off", emphasis = "medium", children, ...rest }) => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-callout", {
		...transformProps({
			type,
			icontheme: iconTheme,
			size,
			arialive: ariaLive,
			emphasis,
			...rest
		}, lowercase),
		version: "2",
		children
	});
};
function GoabCheckbox({ error, checked, indeterminate, disabled, value, description, reveal, onChange, name, children, size = "default", ...rest }) {
	const el = (0, import_react.useRef)(null);
	const _props = transformProps({
		size,
		...rest
	}, lowercase);
	(0, import_react.useEffect)(() => {
		if (!el.current) return;
		const current = el.current;
		const listener = (e) => {
			const detail = e.detail;
			onChange?.({
				...detail,
				event: e
			});
		};
		current.addEventListener("_change", listener);
		return () => {
			current.removeEventListener("_change", listener);
		};
	}, [name, onChange]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("goa-checkbox", {
		ref: el,
		..._props,
		name,
		error: error ? "true" : void 0,
		checked: checked ? "true" : void 0,
		indeterminate: indeterminate ? "true" : void 0,
		disabled: disabled ? "true" : void 0,
		value: typeof value === "boolean" ? value ? "true" : void 0 : value,
		description: typeof description === "string" ? description : void 0,
		version: "2",
		children: [
			children,
			typeof description !== "string" && description && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				slot: "description",
				children: description
			}),
			reveal && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				slot: "reveal",
				children: reveal
			})
		]
	});
}
function GoabCheckboxList({ name, value = [], disabled, error, testId, maxWidth, size = "default", children, onChange, mt, mr, mb, ml }) {
	const el = (0, import_react.useRef)(null);
	(0, import_react.useEffect)(() => {
		if (!el.current) return;
		const current = el.current;
		const listener = (e) => {
			const detail = e.detail;
			onChange?.({
				...detail,
				event: e
			});
		};
		current.addEventListener("_change", listener);
		return () => {
			current.removeEventListener("_change", listener);
		};
	}, [onChange]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-checkbox-list", {
		ref: el,
		name,
		value,
		disabled: disabled ? "true" : void 0,
		error: error ? "true" : void 0,
		testid: testId,
		maxwidth: maxWidth,
		version: "2",
		size,
		mt,
		mr,
		mb,
		ml,
		children
	});
}
var GoabChip = ({ error, deletable, onClick, ...rest }) => {
	const el = (0, import_react.useRef)(null);
	const _props = transformProps(rest, lowercase);
	(0, import_react.useEffect)(() => {
		if (!el.current) return;
		if (!onClick) return;
		const current = el.current;
		const listener = () => {
			onClick?.();
		};
		current.addEventListener("_click", listener);
		return () => {
			current.removeEventListener("_click", listener);
		};
	}, [el, onClick]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-chip", {
		ref: el,
		error: error ? "true" : void 0,
		deletable: deletable ? "true" : void 0,
		..._props
	});
};
var GoabCircularProgress = ({ visible, message: message2, progress, variant, size, testId }) => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-circular-progress", {
		visible: visible ? "true" : void 0,
		message: message2,
		progress,
		variant,
		size,
		testid: testId
	});
};
function GoabContainer({ heading, title, actions, children, ...rest }) {
	const _props = transformProps(rest, lowercase);
	const headingContent = heading || title;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("goa-container", {
		..._props,
		children: [
			headingContent && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				slot: "title",
				children: headingContent
			}),
			children,
			actions && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				slot: "actions",
				children: actions
			})
		]
	});
}
function GoabDataGrid({ keyboardIconVisibility = "visible", keyboardIconPosition = "left", keyboardNav, children }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-data-grid", {
		"keyboard-icon-visibility": keyboardIconVisibility,
		"keyboard-icon-position": keyboardIconPosition,
		"keyboard-nav": keyboardNav,
		children
	});
}
var GoabWorkspaceLayoutScrollState = { NO_SCROLL: "no-scroll" };
function dispatch(el, eventName, detail, opts) {
	if (!el) {
		console.error("dispatch element is null");
		return;
	}
	el.dispatchEvent(new CustomEvent(eventName, {
		composed: true,
		bubbles: opts == null ? void 0 : opts.bubbles,
		detail
	}));
}
function relay(el, eventName, data, opts) {
	if (!el) {
		console.error("dispatch element is null");
		return;
	}
	el.dispatchEvent(new CustomEvent("msg", {
		composed: true,
		bubbles: opts == null ? void 0 : opts.bubbles,
		detail: {
			action: eventName,
			data
		}
	}));
}
var PublicFormController = class {
	constructor(type) {
		__privateAdd(this, _PublicFormController_instances);
		__publicField(this, "state");
		__publicField(this, "_formData");
		__publicField(this, "_formRef");
		__publicField(this, "_isCompleting", false);
		this.type = type;
	}
	init(e) {
		if (this._formRef) {
			console.warn("init: form element has already been set");
			return;
		}
		this._formRef = e.detail.el;
		this.state = {
			uuid: crypto.randomUUID(),
			form: {},
			history: [],
			editting: "",
			status: "not-started"
		};
	}
	initList(e) {
		this._formRef = e.detail.el;
		this.state = [];
	}
	initState(state, callback) {
		relay(this._formRef, "external::init:state", state);
		if (typeof state === "string") this.state = JSON.parse(state);
		else if (!Array.isArray(state)) this.state = state;
		if (callback) setTimeout(callback, 201);
	}
	updateListState(e) {
		const detail = e.detail;
		if (!Array.isArray(detail.data)) return;
		this.state = detail.data;
	}
	updateObjectState(e) {
		var _a, _b;
		if (Array.isArray(this.state)) return;
		const detail = e.detail;
		if (detail.type === "list") this.state = {
			...this.state,
			form: {
				...((_a = this.state) == null ? void 0 : _a.form) || {},
				[detail.id]: detail.data
			}
		};
		else this.state = {
			...this.state,
			...detail.data,
			form: {
				...((_b = this.state) == null ? void 0 : _b.form) || {},
				...detail.data.form
			},
			history: detail.data.history
		};
	}
	getStateList() {
		if (!this.state) return [];
		if (!Array.isArray(this.state)) {
			console.warn("Utils:getStateList: unable to update the state of a non-multi form type", this.state);
			return [];
		}
		if (this.state.length === 0) return [];
		return this.state.map((s) => {
			return Object.values(s.form).filter((item) => {
				var _a;
				return ((_a = item == null ? void 0 : item.data) == null ? void 0 : _a.type) === "details";
			}).map((item) => {
				var _a;
				return item.data.type === "details" && ((_a = item.data) == null ? void 0 : _a.fieldsets) || {};
			}).reduce((acc, item) => {
				for (const [key, value] of Object.entries(item)) acc[key] = value.value;
				return acc;
			}, {});
		});
	}
	getStateValue(group, key) {
		if (Array.isArray(this.state)) {
			console.error("getStateValue: unable to update the state of a multi form type");
			return "";
		}
		if (!this.state) {
			console.error("getStateValue: state has not yet been set");
			return "";
		}
		const data = this.state.form[group].data;
		if (data.type !== "details") return "";
		return data.fieldsets[key].value;
	}
	continueTo(next) {
		if (!next) {
			console.error("continueTo [name] is undefined");
			return;
		}
		relay(this._formRef, "external::continue", { next });
	}
	validate(e, field, validators, options) {
		var _a;
		const { el, state, cancelled } = e.detail;
		const value = (_a = state == null ? void 0 : state[field]) == null ? void 0 : _a.value;
		window.scrollTo({
			top: 0,
			behavior: "smooth"
		});
		if (cancelled) return [true, value];
		for (const validator of validators) {
			const msg = validator(value);
			__privateMethod(this, _PublicFormController_instances, dispatchError_fn).call(this, el, field, msg, options);
			if (msg) return [false, ""];
		}
		return [true, value];
	}
	/**
	* Validates a group of fields ensuring that at least `minPassCount` of the items within the group
	* passes. This is useful in the scenario when n number fields are required out of n+m number of fields.
	*
	* @param {string[]} fields - An array of field names to be validated.
	* @param {Event} e - The event object associated with the validation trigger.
	* @param {FieldValidator[]} validators - An array of validator functions to apply to the fields.
	* @return {[number, Record<string, boolean>]} - Returns back the number of fields that passed and a record of the fields and their pass status.
	*/
	validateGroup(e, fields, validators) {
		let passCount = 0;
		const validGroups = {};
		for (const field of fields) {
			const [_valid] = this.validate(e, field, validators, { grouped: true });
			if (_valid) {
				validGroups[field] = true;
				passCount++;
			}
		}
		return [passCount, validGroups];
	}
	edit(index) {
		relay(this._formRef, "external::alter:state", {
			index,
			operation: "edit"
		});
	}
	remove(index) {
		relay(this._formRef, "external::alter:state", {
			index,
			operation: "remove"
		});
	}
	/**
	* Completes the form and triggers the onComplete callback.
	* This method should be used when you want to complete a form without navigating to a summary page.
	*
	* @important Developers must validate the form before calling this method.
	*
	* @example
	* // Validate first, then complete
	* const [isValid] = this.validate(e, "firstName", [
	*   requiredValidator("First name is required.")
	* ]);
	* if (isValid) {
	*   this.complete();
	* }
	* @returns void
	*/
	complete() {
		if (!this._formRef) {
			console.error("complete: form ref is not set");
			return;
		}
		if (this._isCompleting) {
			console.warn("complete: completion already in progress");
			return;
		}
		this._isCompleting = true;
		relay(this._formRef, "fieldset::submit", null, { bubbles: true });
		this._isCompleting = false;
	}
	/**
	* Completes a subform and returns control to the parent form.
	* This method should be used when working with subforms that need to complete without a summary page.
	*
	* @important Developers must validate the subform before calling this method.
	*
	* @example
	* // Validate first, then complete the subform
	* const [isValid] = this._childFormController.validate(e, "fullName", [
	*   requiredValidator("Please enter the dependent's full name.")
	* ]);
	* if (isValid) {
	*   this._childFormController.completeSubform();
	* }
	* @returns void
	*/
	completeSubform() {
		if (!this._formRef) {
			console.error("completeSubform: form ref is not set");
			return;
		}
		if (this._isCompleting) {
			console.warn("completeSubform: completion already in progress");
			return;
		}
		const formRef = this._formRef;
		this._isCompleting = true;
		const stateChangeHandler = (e) => {
			formRef.removeEventListener("_stateChange", stateChangeHandler);
			dispatch(formRef, "_complete", {}, { bubbles: true });
			this._isCompleting = false;
		};
		formRef.addEventListener("_stateChange", stateChangeHandler);
		dispatch(formRef, "_continue", null, { bubbles: true });
	}
	clean(data) {
		return data.history.reduce((acc, fieldsetId) => {
			acc[fieldsetId] = data.form[fieldsetId];
			return acc;
		}, {});
	}
};
_PublicFormController_instances = /* @__PURE__ */ new WeakSet();
dispatchError_fn = function(el, name, msg, options) {
	el.dispatchEvent(new CustomEvent("msg", {
		composed: true,
		detail: {
			action: "external::set:error",
			data: {
				name,
				msg,
				grouped: options == null ? void 0 : options.grouped
			}
		}
	}));
};
function toDate(argument) {
	const argStr = Object.prototype.toString.call(argument);
	if (argument instanceof Date || typeof argument === "object" && argStr === "[object Date]") return new argument.constructor(+argument);
	else if (typeof argument === "number" || argStr === "[object Number]" || typeof argument === "string" || argStr === "[object String]") return new Date(argument);
	else return /* @__PURE__ */ new Date(NaN);
}
function constructFrom(date, value) {
	if (date instanceof Date) return new date.constructor(value);
	else return new Date(value);
}
function addDays(date, amount) {
	const _date = toDate(date);
	if (isNaN(amount)) return constructFrom(date, NaN);
	if (!amount) return _date;
	_date.setDate(_date.getDate() + amount);
	return _date;
}
function addMonths(date, amount) {
	const _date = toDate(date);
	if (isNaN(amount)) return constructFrom(date, NaN);
	if (!amount) return _date;
	const dayOfMonth = _date.getDate();
	const endOfDesiredMonth = constructFrom(date, _date.getTime());
	endOfDesiredMonth.setMonth(_date.getMonth() + amount + 1, 0);
	if (dayOfMonth >= endOfDesiredMonth.getDate()) return endOfDesiredMonth;
	else {
		_date.setFullYear(endOfDesiredMonth.getFullYear(), endOfDesiredMonth.getMonth(), dayOfMonth);
		return _date;
	}
}
var millisecondsInWeek = 6048e5;
var millisecondsInDay = 864e5;
var millisecondsInMinute = 6e4;
var millisecondsInHour = 36e5;
var defaultOptions = {};
function getDefaultOptions() {
	return defaultOptions;
}
function startOfWeek(date, options) {
	var _a, _b, _c, _d;
	const defaultOptions2 = getDefaultOptions();
	const weekStartsOn = (options == null ? void 0 : options.weekStartsOn) ?? ((_b = (_a = options == null ? void 0 : options.locale) == null ? void 0 : _a.options) == null ? void 0 : _b.weekStartsOn) ?? defaultOptions2.weekStartsOn ?? ((_d = (_c = defaultOptions2.locale) == null ? void 0 : _c.options) == null ? void 0 : _d.weekStartsOn) ?? 0;
	const _date = toDate(date);
	const day = _date.getDay();
	const diff = (day < weekStartsOn ? 7 : 0) + day - weekStartsOn;
	_date.setDate(_date.getDate() - diff);
	_date.setHours(0, 0, 0, 0);
	return _date;
}
function startOfISOWeek(date) {
	return startOfWeek(date, { weekStartsOn: 1 });
}
function getISOWeekYear(date) {
	const _date = toDate(date);
	const year = _date.getFullYear();
	const fourthOfJanuaryOfNextYear = constructFrom(date, 0);
	fourthOfJanuaryOfNextYear.setFullYear(year + 1, 0, 4);
	fourthOfJanuaryOfNextYear.setHours(0, 0, 0, 0);
	const startOfNextYear = startOfISOWeek(fourthOfJanuaryOfNextYear);
	const fourthOfJanuaryOfThisYear = constructFrom(date, 0);
	fourthOfJanuaryOfThisYear.setFullYear(year, 0, 4);
	fourthOfJanuaryOfThisYear.setHours(0, 0, 0, 0);
	const startOfThisYear = startOfISOWeek(fourthOfJanuaryOfThisYear);
	if (_date.getTime() >= startOfNextYear.getTime()) return year + 1;
	else if (_date.getTime() >= startOfThisYear.getTime()) return year;
	else return year - 1;
}
function startOfDay(date) {
	const _date = toDate(date);
	_date.setHours(0, 0, 0, 0);
	return _date;
}
function getTimezoneOffsetInMilliseconds(date) {
	const _date = toDate(date);
	const utcDate = new Date(Date.UTC(_date.getFullYear(), _date.getMonth(), _date.getDate(), _date.getHours(), _date.getMinutes(), _date.getSeconds(), _date.getMilliseconds()));
	utcDate.setUTCFullYear(_date.getFullYear());
	return +date - +utcDate;
}
function differenceInCalendarDays(dateLeft, dateRight) {
	const startOfDayLeft = startOfDay(dateLeft);
	const startOfDayRight = startOfDay(dateRight);
	const timestampLeft = +startOfDayLeft - getTimezoneOffsetInMilliseconds(startOfDayLeft);
	const timestampRight = +startOfDayRight - getTimezoneOffsetInMilliseconds(startOfDayRight);
	return Math.round((timestampLeft - timestampRight) / millisecondsInDay);
}
function startOfISOWeekYear(date) {
	const year = getISOWeekYear(date);
	const fourthOfJanuary = constructFrom(date, 0);
	fourthOfJanuary.setFullYear(year, 0, 4);
	fourthOfJanuary.setHours(0, 0, 0, 0);
	return startOfISOWeek(fourthOfJanuary);
}
function isSameDay(dateLeft, dateRight) {
	const dateLeftStartOfDay = startOfDay(dateLeft);
	const dateRightStartOfDay = startOfDay(dateRight);
	return +dateLeftStartOfDay === +dateRightStartOfDay;
}
function isDate(value) {
	return value instanceof Date || typeof value === "object" && Object.prototype.toString.call(value) === "[object Date]";
}
function isValid(date) {
	if (!isDate(date) && typeof date !== "number") return false;
	const _date = toDate(date);
	return !isNaN(Number(_date));
}
function startOfYear(date) {
	const cleanDate = toDate(date);
	const _date = constructFrom(date, 0);
	_date.setFullYear(cleanDate.getFullYear(), 0, 1);
	_date.setHours(0, 0, 0, 0);
	return _date;
}
var formatDistanceLocale = {
	lessThanXSeconds: {
		one: "less than a second",
		other: "less than {{count}} seconds"
	},
	xSeconds: {
		one: "1 second",
		other: "{{count}} seconds"
	},
	halfAMinute: "half a minute",
	lessThanXMinutes: {
		one: "less than a minute",
		other: "less than {{count}} minutes"
	},
	xMinutes: {
		one: "1 minute",
		other: "{{count}} minutes"
	},
	aboutXHours: {
		one: "about 1 hour",
		other: "about {{count}} hours"
	},
	xHours: {
		one: "1 hour",
		other: "{{count}} hours"
	},
	xDays: {
		one: "1 day",
		other: "{{count}} days"
	},
	aboutXWeeks: {
		one: "about 1 week",
		other: "about {{count}} weeks"
	},
	xWeeks: {
		one: "1 week",
		other: "{{count}} weeks"
	},
	aboutXMonths: {
		one: "about 1 month",
		other: "about {{count}} months"
	},
	xMonths: {
		one: "1 month",
		other: "{{count}} months"
	},
	aboutXYears: {
		one: "about 1 year",
		other: "about {{count}} years"
	},
	xYears: {
		one: "1 year",
		other: "{{count}} years"
	},
	overXYears: {
		one: "over 1 year",
		other: "over {{count}} years"
	},
	almostXYears: {
		one: "almost 1 year",
		other: "almost {{count}} years"
	}
};
var formatDistance = (token, count, options) => {
	let result;
	const tokenValue = formatDistanceLocale[token];
	if (typeof tokenValue === "string") result = tokenValue;
	else if (count === 1) result = tokenValue.one;
	else result = tokenValue.other.replace("{{count}}", count.toString());
	if (options == null ? void 0 : options.addSuffix) if (options.comparison && options.comparison > 0) return "in " + result;
	else return result + " ago";
	return result;
};
function buildFormatLongFn(args) {
	return (options = {}) => {
		const width = options.width ? String(options.width) : args.defaultWidth;
		return args.formats[width] || args.formats[args.defaultWidth];
	};
}
var formatLong = {
	date: buildFormatLongFn({
		formats: {
			full: "EEEE, MMMM do, y",
			long: "MMMM do, y",
			medium: "MMM d, y",
			short: "MM/dd/yyyy"
		},
		defaultWidth: "full"
	}),
	time: buildFormatLongFn({
		formats: {
			full: "h:mm:ss a zzzz",
			long: "h:mm:ss a z",
			medium: "h:mm:ss a",
			short: "h:mm a"
		},
		defaultWidth: "full"
	}),
	dateTime: buildFormatLongFn({
		formats: {
			full: "{{date}} 'at' {{time}}",
			long: "{{date}} 'at' {{time}}",
			medium: "{{date}}, {{time}}",
			short: "{{date}}, {{time}}"
		},
		defaultWidth: "full"
	})
};
var formatRelativeLocale = {
	lastWeek: "'last' eeee 'at' p",
	yesterday: "'yesterday at' p",
	today: "'today at' p",
	tomorrow: "'tomorrow at' p",
	nextWeek: "eeee 'at' p",
	other: "P"
};
var formatRelative = (token, _date, _baseDate, _options) => formatRelativeLocale[token];
function buildLocalizeFn(args) {
	return (value, options) => {
		const context = (options == null ? void 0 : options.context) ? String(options.context) : "standalone";
		let valuesArray;
		if (context === "formatting" && args.formattingValues) {
			const defaultWidth = args.defaultFormattingWidth || args.defaultWidth;
			const width = (options == null ? void 0 : options.width) ? String(options.width) : defaultWidth;
			valuesArray = args.formattingValues[width] || args.formattingValues[defaultWidth];
		} else {
			const defaultWidth = args.defaultWidth;
			const width = (options == null ? void 0 : options.width) ? String(options.width) : args.defaultWidth;
			valuesArray = args.values[width] || args.values[defaultWidth];
		}
		const index = args.argumentCallback ? args.argumentCallback(value) : value;
		return valuesArray[index];
	};
}
var eraValues = {
	narrow: ["B", "A"],
	abbreviated: ["BC", "AD"],
	wide: ["Before Christ", "Anno Domini"]
};
var quarterValues = {
	narrow: [
		"1",
		"2",
		"3",
		"4"
	],
	abbreviated: [
		"Q1",
		"Q2",
		"Q3",
		"Q4"
	],
	wide: [
		"1st quarter",
		"2nd quarter",
		"3rd quarter",
		"4th quarter"
	]
};
var monthValues = {
	narrow: [
		"J",
		"F",
		"M",
		"A",
		"M",
		"J",
		"J",
		"A",
		"S",
		"O",
		"N",
		"D"
	],
	abbreviated: [
		"Jan",
		"Feb",
		"Mar",
		"Apr",
		"May",
		"Jun",
		"Jul",
		"Aug",
		"Sep",
		"Oct",
		"Nov",
		"Dec"
	],
	wide: [
		"January",
		"February",
		"March",
		"April",
		"May",
		"June",
		"July",
		"August",
		"September",
		"October",
		"November",
		"December"
	]
};
var dayValues = {
	narrow: [
		"S",
		"M",
		"T",
		"W",
		"T",
		"F",
		"S"
	],
	short: [
		"Su",
		"Mo",
		"Tu",
		"We",
		"Th",
		"Fr",
		"Sa"
	],
	abbreviated: [
		"Sun",
		"Mon",
		"Tue",
		"Wed",
		"Thu",
		"Fri",
		"Sat"
	],
	wide: [
		"Sunday",
		"Monday",
		"Tuesday",
		"Wednesday",
		"Thursday",
		"Friday",
		"Saturday"
	]
};
var dayPeriodValues = {
	narrow: {
		am: "a",
		pm: "p",
		midnight: "mi",
		noon: "n",
		morning: "morning",
		afternoon: "afternoon",
		evening: "evening",
		night: "night"
	},
	abbreviated: {
		am: "AM",
		pm: "PM",
		midnight: "midnight",
		noon: "noon",
		morning: "morning",
		afternoon: "afternoon",
		evening: "evening",
		night: "night"
	},
	wide: {
		am: "a.m.",
		pm: "p.m.",
		midnight: "midnight",
		noon: "noon",
		morning: "morning",
		afternoon: "afternoon",
		evening: "evening",
		night: "night"
	}
};
var formattingDayPeriodValues = {
	narrow: {
		am: "a",
		pm: "p",
		midnight: "mi",
		noon: "n",
		morning: "in the morning",
		afternoon: "in the afternoon",
		evening: "in the evening",
		night: "at night"
	},
	abbreviated: {
		am: "AM",
		pm: "PM",
		midnight: "midnight",
		noon: "noon",
		morning: "in the morning",
		afternoon: "in the afternoon",
		evening: "in the evening",
		night: "at night"
	},
	wide: {
		am: "a.m.",
		pm: "p.m.",
		midnight: "midnight",
		noon: "noon",
		morning: "in the morning",
		afternoon: "in the afternoon",
		evening: "in the evening",
		night: "at night"
	}
};
var ordinalNumber = (dirtyNumber, _options) => {
	const number = Number(dirtyNumber);
	const rem100 = number % 100;
	if (rem100 > 20 || rem100 < 10) switch (rem100 % 10) {
		case 1: return number + "st";
		case 2: return number + "nd";
		case 3: return number + "rd";
	}
	return number + "th";
};
var localize = {
	ordinalNumber,
	era: buildLocalizeFn({
		values: eraValues,
		defaultWidth: "wide"
	}),
	quarter: buildLocalizeFn({
		values: quarterValues,
		defaultWidth: "wide",
		argumentCallback: (quarter) => quarter - 1
	}),
	month: buildLocalizeFn({
		values: monthValues,
		defaultWidth: "wide"
	}),
	day: buildLocalizeFn({
		values: dayValues,
		defaultWidth: "wide"
	}),
	dayPeriod: buildLocalizeFn({
		values: dayPeriodValues,
		defaultWidth: "wide",
		formattingValues: formattingDayPeriodValues,
		defaultFormattingWidth: "wide"
	})
};
function buildMatchFn(args) {
	return (string, options = {}) => {
		const width = options.width;
		const matchPattern = width && args.matchPatterns[width] || args.matchPatterns[args.defaultMatchWidth];
		const matchResult = string.match(matchPattern);
		if (!matchResult) return null;
		const matchedString = matchResult[0];
		const parsePatterns = width && args.parsePatterns[width] || args.parsePatterns[args.defaultParseWidth];
		const key = Array.isArray(parsePatterns) ? findIndex(parsePatterns, (pattern) => pattern.test(matchedString)) : findKey(parsePatterns, (pattern) => pattern.test(matchedString));
		let value;
		value = args.valueCallback ? args.valueCallback(key) : key;
		value = options.valueCallback ? options.valueCallback(value) : value;
		const rest = string.slice(matchedString.length);
		return {
			value,
			rest
		};
	};
}
function findKey(object, predicate) {
	for (const key in object) if (Object.prototype.hasOwnProperty.call(object, key) && predicate(object[key])) return key;
}
function findIndex(array, predicate) {
	for (let key = 0; key < array.length; key++) if (predicate(array[key])) return key;
}
function buildMatchPatternFn(args) {
	return (string, options = {}) => {
		const matchResult = string.match(args.matchPattern);
		if (!matchResult) return null;
		const matchedString = matchResult[0];
		const parseResult = string.match(args.parsePattern);
		if (!parseResult) return null;
		let value = args.valueCallback ? args.valueCallback(parseResult[0]) : parseResult[0];
		value = options.valueCallback ? options.valueCallback(value) : value;
		const rest = string.slice(matchedString.length);
		return {
			value,
			rest
		};
	};
}
var enUS = {
	code: "en-US",
	formatDistance,
	formatLong,
	formatRelative,
	localize,
	match: {
		ordinalNumber: buildMatchPatternFn({
			matchPattern: /^(\d+)(th|st|nd|rd)?/i,
			parsePattern: /\d+/i,
			valueCallback: (value) => parseInt(value, 10)
		}),
		era: buildMatchFn({
			matchPatterns: {
				narrow: /^(b|a)/i,
				abbreviated: /^(b\.?\s?c\.?|b\.?\s?c\.?\s?e\.?|a\.?\s?d\.?|c\.?\s?e\.?)/i,
				wide: /^(before christ|before common era|anno domini|common era)/i
			},
			defaultMatchWidth: "wide",
			parsePatterns: { any: [/^b/i, /^(a|c)/i] },
			defaultParseWidth: "any"
		}),
		quarter: buildMatchFn({
			matchPatterns: {
				narrow: /^[1234]/i,
				abbreviated: /^q[1234]/i,
				wide: /^[1234](th|st|nd|rd)? quarter/i
			},
			defaultMatchWidth: "wide",
			parsePatterns: { any: [
				/1/i,
				/2/i,
				/3/i,
				/4/i
			] },
			defaultParseWidth: "any",
			valueCallback: (index) => index + 1
		}),
		month: buildMatchFn({
			matchPatterns: {
				narrow: /^[jfmasond]/i,
				abbreviated: /^(jan|feb|mar|apr|may|jun|jul|aug|sep|oct|nov|dec)/i,
				wide: /^(january|february|march|april|may|june|july|august|september|october|november|december)/i
			},
			defaultMatchWidth: "wide",
			parsePatterns: {
				narrow: [
					/^j/i,
					/^f/i,
					/^m/i,
					/^a/i,
					/^m/i,
					/^j/i,
					/^j/i,
					/^a/i,
					/^s/i,
					/^o/i,
					/^n/i,
					/^d/i
				],
				any: [
					/^ja/i,
					/^f/i,
					/^mar/i,
					/^ap/i,
					/^may/i,
					/^jun/i,
					/^jul/i,
					/^au/i,
					/^s/i,
					/^o/i,
					/^n/i,
					/^d/i
				]
			},
			defaultParseWidth: "any"
		}),
		day: buildMatchFn({
			matchPatterns: {
				narrow: /^[smtwf]/i,
				short: /^(su|mo|tu|we|th|fr|sa)/i,
				abbreviated: /^(sun|mon|tue|wed|thu|fri|sat)/i,
				wide: /^(sunday|monday|tuesday|wednesday|thursday|friday|saturday)/i
			},
			defaultMatchWidth: "wide",
			parsePatterns: {
				narrow: [
					/^s/i,
					/^m/i,
					/^t/i,
					/^w/i,
					/^t/i,
					/^f/i,
					/^s/i
				],
				any: [
					/^su/i,
					/^m/i,
					/^tu/i,
					/^w/i,
					/^th/i,
					/^f/i,
					/^sa/i
				]
			},
			defaultParseWidth: "any"
		}),
		dayPeriod: buildMatchFn({
			matchPatterns: {
				narrow: /^(a|p|mi|n|(in the|at) (morning|afternoon|evening|night))/i,
				any: /^([ap]\.?\s?m\.?|midnight|noon|(in the|at) (morning|afternoon|evening|night))/i
			},
			defaultMatchWidth: "any",
			parsePatterns: { any: {
				am: /^a/i,
				pm: /^p/i,
				midnight: /^mi/i,
				noon: /^no/i,
				morning: /morning/i,
				afternoon: /afternoon/i,
				evening: /evening/i,
				night: /night/i
			} },
			defaultParseWidth: "any"
		})
	},
	options: {
		weekStartsOn: 0,
		firstWeekContainsDate: 1
	}
};
function getDayOfYear(date) {
	const _date = toDate(date);
	return differenceInCalendarDays(_date, startOfYear(_date)) + 1;
}
function getISOWeek(date) {
	const _date = toDate(date);
	const diff = +startOfISOWeek(_date) - +startOfISOWeekYear(_date);
	return Math.round(diff / millisecondsInWeek) + 1;
}
function getWeekYear(date, options) {
	var _a, _b, _c, _d;
	const _date = toDate(date);
	const year = _date.getFullYear();
	const defaultOptions2 = getDefaultOptions();
	const firstWeekContainsDate = (options == null ? void 0 : options.firstWeekContainsDate) ?? ((_b = (_a = options == null ? void 0 : options.locale) == null ? void 0 : _a.options) == null ? void 0 : _b.firstWeekContainsDate) ?? defaultOptions2.firstWeekContainsDate ?? ((_d = (_c = defaultOptions2.locale) == null ? void 0 : _c.options) == null ? void 0 : _d.firstWeekContainsDate) ?? 1;
	const firstWeekOfNextYear = constructFrom(date, 0);
	firstWeekOfNextYear.setFullYear(year + 1, 0, firstWeekContainsDate);
	firstWeekOfNextYear.setHours(0, 0, 0, 0);
	const startOfNextYear = startOfWeek(firstWeekOfNextYear, options);
	const firstWeekOfThisYear = constructFrom(date, 0);
	firstWeekOfThisYear.setFullYear(year, 0, firstWeekContainsDate);
	firstWeekOfThisYear.setHours(0, 0, 0, 0);
	const startOfThisYear = startOfWeek(firstWeekOfThisYear, options);
	if (_date.getTime() >= startOfNextYear.getTime()) return year + 1;
	else if (_date.getTime() >= startOfThisYear.getTime()) return year;
	else return year - 1;
}
function startOfWeekYear(date, options) {
	var _a, _b, _c, _d;
	const defaultOptions2 = getDefaultOptions();
	const firstWeekContainsDate = (options == null ? void 0 : options.firstWeekContainsDate) ?? ((_b = (_a = options == null ? void 0 : options.locale) == null ? void 0 : _a.options) == null ? void 0 : _b.firstWeekContainsDate) ?? defaultOptions2.firstWeekContainsDate ?? ((_d = (_c = defaultOptions2.locale) == null ? void 0 : _c.options) == null ? void 0 : _d.firstWeekContainsDate) ?? 1;
	const year = getWeekYear(date, options);
	const firstWeek = constructFrom(date, 0);
	firstWeek.setFullYear(year, 0, firstWeekContainsDate);
	firstWeek.setHours(0, 0, 0, 0);
	return startOfWeek(firstWeek, options);
}
function getWeek(date, options) {
	const _date = toDate(date);
	const diff = +startOfWeek(_date, options) - +startOfWeekYear(_date, options);
	return Math.round(diff / millisecondsInWeek) + 1;
}
function addLeadingZeros(number, targetLength) {
	return (number < 0 ? "-" : "") + Math.abs(number).toString().padStart(targetLength, "0");
}
var lightFormatters = {
	y(date, token) {
		const signedYear = date.getFullYear();
		const year = signedYear > 0 ? signedYear : 1 - signedYear;
		return addLeadingZeros(token === "yy" ? year % 100 : year, token.length);
	},
	M(date, token) {
		const month = date.getMonth();
		return token === "M" ? String(month + 1) : addLeadingZeros(month + 1, 2);
	},
	d(date, token) {
		return addLeadingZeros(date.getDate(), token.length);
	},
	a(date, token) {
		const dayPeriodEnumValue = date.getHours() / 12 >= 1 ? "pm" : "am";
		switch (token) {
			case "a":
			case "aa": return dayPeriodEnumValue.toUpperCase();
			case "aaa": return dayPeriodEnumValue;
			case "aaaaa": return dayPeriodEnumValue[0];
			default: return dayPeriodEnumValue === "am" ? "a.m." : "p.m.";
		}
	},
	h(date, token) {
		return addLeadingZeros(date.getHours() % 12 || 12, token.length);
	},
	H(date, token) {
		return addLeadingZeros(date.getHours(), token.length);
	},
	m(date, token) {
		return addLeadingZeros(date.getMinutes(), token.length);
	},
	s(date, token) {
		return addLeadingZeros(date.getSeconds(), token.length);
	},
	S(date, token) {
		const numberOfDigits = token.length;
		const milliseconds = date.getMilliseconds();
		return addLeadingZeros(Math.trunc(milliseconds * Math.pow(10, numberOfDigits - 3)), token.length);
	}
};
var dayPeriodEnum = {
	midnight: "midnight",
	noon: "noon",
	morning: "morning",
	afternoon: "afternoon",
	evening: "evening",
	night: "night"
};
var formatters = {
	G: function(date, token, localize2) {
		const era = date.getFullYear() > 0 ? 1 : 0;
		switch (token) {
			case "G":
			case "GG":
			case "GGG": return localize2.era(era, { width: "abbreviated" });
			case "GGGGG": return localize2.era(era, { width: "narrow" });
			default: return localize2.era(era, { width: "wide" });
		}
	},
	y: function(date, token, localize2) {
		if (token === "yo") {
			const signedYear = date.getFullYear();
			const year = signedYear > 0 ? signedYear : 1 - signedYear;
			return localize2.ordinalNumber(year, { unit: "year" });
		}
		return lightFormatters.y(date, token);
	},
	Y: function(date, token, localize2, options) {
		const signedWeekYear = getWeekYear(date, options);
		const weekYear = signedWeekYear > 0 ? signedWeekYear : 1 - signedWeekYear;
		if (token === "YY") return addLeadingZeros(weekYear % 100, 2);
		if (token === "Yo") return localize2.ordinalNumber(weekYear, { unit: "year" });
		return addLeadingZeros(weekYear, token.length);
	},
	R: function(date, token) {
		return addLeadingZeros(getISOWeekYear(date), token.length);
	},
	u: function(date, token) {
		return addLeadingZeros(date.getFullYear(), token.length);
	},
	Q: function(date, token, localize2) {
		const quarter = Math.ceil((date.getMonth() + 1) / 3);
		switch (token) {
			case "Q": return String(quarter);
			case "QQ": return addLeadingZeros(quarter, 2);
			case "Qo": return localize2.ordinalNumber(quarter, { unit: "quarter" });
			case "QQQ": return localize2.quarter(quarter, {
				width: "abbreviated",
				context: "formatting"
			});
			case "QQQQQ": return localize2.quarter(quarter, {
				width: "narrow",
				context: "formatting"
			});
			default: return localize2.quarter(quarter, {
				width: "wide",
				context: "formatting"
			});
		}
	},
	q: function(date, token, localize2) {
		const quarter = Math.ceil((date.getMonth() + 1) / 3);
		switch (token) {
			case "q": return String(quarter);
			case "qq": return addLeadingZeros(quarter, 2);
			case "qo": return localize2.ordinalNumber(quarter, { unit: "quarter" });
			case "qqq": return localize2.quarter(quarter, {
				width: "abbreviated",
				context: "standalone"
			});
			case "qqqqq": return localize2.quarter(quarter, {
				width: "narrow",
				context: "standalone"
			});
			default: return localize2.quarter(quarter, {
				width: "wide",
				context: "standalone"
			});
		}
	},
	M: function(date, token, localize2) {
		const month = date.getMonth();
		switch (token) {
			case "M":
			case "MM": return lightFormatters.M(date, token);
			case "Mo": return localize2.ordinalNumber(month + 1, { unit: "month" });
			case "MMM": return localize2.month(month, {
				width: "abbreviated",
				context: "formatting"
			});
			case "MMMMM": return localize2.month(month, {
				width: "narrow",
				context: "formatting"
			});
			default: return localize2.month(month, {
				width: "wide",
				context: "formatting"
			});
		}
	},
	L: function(date, token, localize2) {
		const month = date.getMonth();
		switch (token) {
			case "L": return String(month + 1);
			case "LL": return addLeadingZeros(month + 1, 2);
			case "Lo": return localize2.ordinalNumber(month + 1, { unit: "month" });
			case "LLL": return localize2.month(month, {
				width: "abbreviated",
				context: "standalone"
			});
			case "LLLLL": return localize2.month(month, {
				width: "narrow",
				context: "standalone"
			});
			default: return localize2.month(month, {
				width: "wide",
				context: "standalone"
			});
		}
	},
	w: function(date, token, localize2, options) {
		const week = getWeek(date, options);
		if (token === "wo") return localize2.ordinalNumber(week, { unit: "week" });
		return addLeadingZeros(week, token.length);
	},
	I: function(date, token, localize2) {
		const isoWeek = getISOWeek(date);
		if (token === "Io") return localize2.ordinalNumber(isoWeek, { unit: "week" });
		return addLeadingZeros(isoWeek, token.length);
	},
	d: function(date, token, localize2) {
		if (token === "do") return localize2.ordinalNumber(date.getDate(), { unit: "date" });
		return lightFormatters.d(date, token);
	},
	D: function(date, token, localize2) {
		const dayOfYear = getDayOfYear(date);
		if (token === "Do") return localize2.ordinalNumber(dayOfYear, { unit: "dayOfYear" });
		return addLeadingZeros(dayOfYear, token.length);
	},
	E: function(date, token, localize2) {
		const dayOfWeek = date.getDay();
		switch (token) {
			case "E":
			case "EE":
			case "EEE": return localize2.day(dayOfWeek, {
				width: "abbreviated",
				context: "formatting"
			});
			case "EEEEE": return localize2.day(dayOfWeek, {
				width: "narrow",
				context: "formatting"
			});
			case "EEEEEE": return localize2.day(dayOfWeek, {
				width: "short",
				context: "formatting"
			});
			default: return localize2.day(dayOfWeek, {
				width: "wide",
				context: "formatting"
			});
		}
	},
	e: function(date, token, localize2, options) {
		const dayOfWeek = date.getDay();
		const localDayOfWeek = (dayOfWeek - options.weekStartsOn + 8) % 7 || 7;
		switch (token) {
			case "e": return String(localDayOfWeek);
			case "ee": return addLeadingZeros(localDayOfWeek, 2);
			case "eo": return localize2.ordinalNumber(localDayOfWeek, { unit: "day" });
			case "eee": return localize2.day(dayOfWeek, {
				width: "abbreviated",
				context: "formatting"
			});
			case "eeeee": return localize2.day(dayOfWeek, {
				width: "narrow",
				context: "formatting"
			});
			case "eeeeee": return localize2.day(dayOfWeek, {
				width: "short",
				context: "formatting"
			});
			default: return localize2.day(dayOfWeek, {
				width: "wide",
				context: "formatting"
			});
		}
	},
	c: function(date, token, localize2, options) {
		const dayOfWeek = date.getDay();
		const localDayOfWeek = (dayOfWeek - options.weekStartsOn + 8) % 7 || 7;
		switch (token) {
			case "c": return String(localDayOfWeek);
			case "cc": return addLeadingZeros(localDayOfWeek, token.length);
			case "co": return localize2.ordinalNumber(localDayOfWeek, { unit: "day" });
			case "ccc": return localize2.day(dayOfWeek, {
				width: "abbreviated",
				context: "standalone"
			});
			case "ccccc": return localize2.day(dayOfWeek, {
				width: "narrow",
				context: "standalone"
			});
			case "cccccc": return localize2.day(dayOfWeek, {
				width: "short",
				context: "standalone"
			});
			default: return localize2.day(dayOfWeek, {
				width: "wide",
				context: "standalone"
			});
		}
	},
	i: function(date, token, localize2) {
		const dayOfWeek = date.getDay();
		const isoDayOfWeek = dayOfWeek === 0 ? 7 : dayOfWeek;
		switch (token) {
			case "i": return String(isoDayOfWeek);
			case "ii": return addLeadingZeros(isoDayOfWeek, token.length);
			case "io": return localize2.ordinalNumber(isoDayOfWeek, { unit: "day" });
			case "iii": return localize2.day(dayOfWeek, {
				width: "abbreviated",
				context: "formatting"
			});
			case "iiiii": return localize2.day(dayOfWeek, {
				width: "narrow",
				context: "formatting"
			});
			case "iiiiii": return localize2.day(dayOfWeek, {
				width: "short",
				context: "formatting"
			});
			default: return localize2.day(dayOfWeek, {
				width: "wide",
				context: "formatting"
			});
		}
	},
	a: function(date, token, localize2) {
		const dayPeriodEnumValue = date.getHours() / 12 >= 1 ? "pm" : "am";
		switch (token) {
			case "a":
			case "aa": return localize2.dayPeriod(dayPeriodEnumValue, {
				width: "abbreviated",
				context: "formatting"
			});
			case "aaa": return localize2.dayPeriod(dayPeriodEnumValue, {
				width: "abbreviated",
				context: "formatting"
			}).toLowerCase();
			case "aaaaa": return localize2.dayPeriod(dayPeriodEnumValue, {
				width: "narrow",
				context: "formatting"
			});
			default: return localize2.dayPeriod(dayPeriodEnumValue, {
				width: "wide",
				context: "formatting"
			});
		}
	},
	b: function(date, token, localize2) {
		const hours = date.getHours();
		let dayPeriodEnumValue;
		if (hours === 12) dayPeriodEnumValue = dayPeriodEnum.noon;
		else if (hours === 0) dayPeriodEnumValue = dayPeriodEnum.midnight;
		else dayPeriodEnumValue = hours / 12 >= 1 ? "pm" : "am";
		switch (token) {
			case "b":
			case "bb": return localize2.dayPeriod(dayPeriodEnumValue, {
				width: "abbreviated",
				context: "formatting"
			});
			case "bbb": return localize2.dayPeriod(dayPeriodEnumValue, {
				width: "abbreviated",
				context: "formatting"
			}).toLowerCase();
			case "bbbbb": return localize2.dayPeriod(dayPeriodEnumValue, {
				width: "narrow",
				context: "formatting"
			});
			default: return localize2.dayPeriod(dayPeriodEnumValue, {
				width: "wide",
				context: "formatting"
			});
		}
	},
	B: function(date, token, localize2) {
		const hours = date.getHours();
		let dayPeriodEnumValue;
		if (hours >= 17) dayPeriodEnumValue = dayPeriodEnum.evening;
		else if (hours >= 12) dayPeriodEnumValue = dayPeriodEnum.afternoon;
		else if (hours >= 4) dayPeriodEnumValue = dayPeriodEnum.morning;
		else dayPeriodEnumValue = dayPeriodEnum.night;
		switch (token) {
			case "B":
			case "BB":
			case "BBB": return localize2.dayPeriod(dayPeriodEnumValue, {
				width: "abbreviated",
				context: "formatting"
			});
			case "BBBBB": return localize2.dayPeriod(dayPeriodEnumValue, {
				width: "narrow",
				context: "formatting"
			});
			default: return localize2.dayPeriod(dayPeriodEnumValue, {
				width: "wide",
				context: "formatting"
			});
		}
	},
	h: function(date, token, localize2) {
		if (token === "ho") {
			let hours = date.getHours() % 12;
			if (hours === 0) hours = 12;
			return localize2.ordinalNumber(hours, { unit: "hour" });
		}
		return lightFormatters.h(date, token);
	},
	H: function(date, token, localize2) {
		if (token === "Ho") return localize2.ordinalNumber(date.getHours(), { unit: "hour" });
		return lightFormatters.H(date, token);
	},
	K: function(date, token, localize2) {
		const hours = date.getHours() % 12;
		if (token === "Ko") return localize2.ordinalNumber(hours, { unit: "hour" });
		return addLeadingZeros(hours, token.length);
	},
	k: function(date, token, localize2) {
		let hours = date.getHours();
		if (hours === 0) hours = 24;
		if (token === "ko") return localize2.ordinalNumber(hours, { unit: "hour" });
		return addLeadingZeros(hours, token.length);
	},
	m: function(date, token, localize2) {
		if (token === "mo") return localize2.ordinalNumber(date.getMinutes(), { unit: "minute" });
		return lightFormatters.m(date, token);
	},
	s: function(date, token, localize2) {
		if (token === "so") return localize2.ordinalNumber(date.getSeconds(), { unit: "second" });
		return lightFormatters.s(date, token);
	},
	S: function(date, token) {
		return lightFormatters.S(date, token);
	},
	X: function(date, token, _localize) {
		const timezoneOffset = date.getTimezoneOffset();
		if (timezoneOffset === 0) return "Z";
		switch (token) {
			case "X": return formatTimezoneWithOptionalMinutes(timezoneOffset);
			case "XXXX":
			case "XX": return formatTimezone(timezoneOffset);
			default: return formatTimezone(timezoneOffset, ":");
		}
	},
	x: function(date, token, _localize) {
		const timezoneOffset = date.getTimezoneOffset();
		switch (token) {
			case "x": return formatTimezoneWithOptionalMinutes(timezoneOffset);
			case "xxxx":
			case "xx": return formatTimezone(timezoneOffset);
			default: return formatTimezone(timezoneOffset, ":");
		}
	},
	O: function(date, token, _localize) {
		const timezoneOffset = date.getTimezoneOffset();
		switch (token) {
			case "O":
			case "OO":
			case "OOO": return "GMT" + formatTimezoneShort(timezoneOffset, ":");
			default: return "GMT" + formatTimezone(timezoneOffset, ":");
		}
	},
	z: function(date, token, _localize) {
		const timezoneOffset = date.getTimezoneOffset();
		switch (token) {
			case "z":
			case "zz":
			case "zzz": return "GMT" + formatTimezoneShort(timezoneOffset, ":");
			default: return "GMT" + formatTimezone(timezoneOffset, ":");
		}
	},
	t: function(date, token, _localize) {
		return addLeadingZeros(Math.trunc(date.getTime() / 1e3), token.length);
	},
	T: function(date, token, _localize) {
		return addLeadingZeros(date.getTime(), token.length);
	}
};
function formatTimezoneShort(offset, delimiter = "") {
	const sign = offset > 0 ? "-" : "+";
	const absOffset = Math.abs(offset);
	const hours = Math.trunc(absOffset / 60);
	const minutes = absOffset % 60;
	if (minutes === 0) return sign + String(hours);
	return sign + String(hours) + delimiter + addLeadingZeros(minutes, 2);
}
function formatTimezoneWithOptionalMinutes(offset, delimiter) {
	if (offset % 60 === 0) return (offset > 0 ? "-" : "+") + addLeadingZeros(Math.abs(offset) / 60, 2);
	return formatTimezone(offset, delimiter);
}
function formatTimezone(offset, delimiter = "") {
	const sign = offset > 0 ? "-" : "+";
	const absOffset = Math.abs(offset);
	const hours = addLeadingZeros(Math.trunc(absOffset / 60), 2);
	const minutes = addLeadingZeros(absOffset % 60, 2);
	return sign + hours + delimiter + minutes;
}
var dateLongFormatter = (pattern, formatLong2) => {
	switch (pattern) {
		case "P": return formatLong2.date({ width: "short" });
		case "PP": return formatLong2.date({ width: "medium" });
		case "PPP": return formatLong2.date({ width: "long" });
		default: return formatLong2.date({ width: "full" });
	}
};
var timeLongFormatter = (pattern, formatLong2) => {
	switch (pattern) {
		case "p": return formatLong2.time({ width: "short" });
		case "pp": return formatLong2.time({ width: "medium" });
		case "ppp": return formatLong2.time({ width: "long" });
		default: return formatLong2.time({ width: "full" });
	}
};
var dateTimeLongFormatter = (pattern, formatLong2) => {
	const matchResult = pattern.match(/(P+)(p+)?/) || [];
	const datePattern = matchResult[1];
	const timePattern = matchResult[2];
	if (!timePattern) return dateLongFormatter(pattern, formatLong2);
	let dateTimeFormat;
	switch (datePattern) {
		case "P":
			dateTimeFormat = formatLong2.dateTime({ width: "short" });
			break;
		case "PP":
			dateTimeFormat = formatLong2.dateTime({ width: "medium" });
			break;
		case "PPP":
			dateTimeFormat = formatLong2.dateTime({ width: "long" });
			break;
		default:
			dateTimeFormat = formatLong2.dateTime({ width: "full" });
			break;
	}
	return dateTimeFormat.replace("{{date}}", dateLongFormatter(datePattern, formatLong2)).replace("{{time}}", timeLongFormatter(timePattern, formatLong2));
};
var longFormatters = {
	p: timeLongFormatter,
	P: dateTimeLongFormatter
};
var dayOfYearTokenRE = /^D+$/;
var weekYearTokenRE = /^Y+$/;
var throwTokens = [
	"D",
	"DD",
	"YY",
	"YYYY"
];
function isProtectedDayOfYearToken(token) {
	return dayOfYearTokenRE.test(token);
}
function isProtectedWeekYearToken(token) {
	return weekYearTokenRE.test(token);
}
function warnOrThrowProtectedError(token, format2, input) {
	const _message = message(token, format2, input);
	console.warn(_message);
	if (throwTokens.includes(token)) throw new RangeError(_message);
}
function message(token, format2, input) {
	const subject = token[0] === "Y" ? "years" : "days of the month";
	return `Use \`${token.toLowerCase()}\` instead of \`${token}\` (in \`${format2}\`) for formatting ${subject} to the input \`${input}\`; see: https://github.com/date-fns/date-fns/blob/master/docs/unicodeTokens.md`;
}
var formattingTokensRegExp = /[yYQqMLwIdDecihHKkms]o|(\w)\1*|''|'(''|[^'])+('|$)|./g;
var longFormattingTokensRegExp = /P+p+|P+|p+|''|'(''|[^'])+('|$)|./g;
var escapedStringRegExp = /^'([^]*?)'?$/;
var doubleQuoteRegExp = /''/g;
var unescapedLatinCharacterRegExp = /[a-zA-Z]/;
function format(date, formatStr, options) {
	var _a, _b, _c, _d;
	const defaultOptions2 = getDefaultOptions();
	const locale = defaultOptions2.locale ?? enUS;
	const firstWeekContainsDate = defaultOptions2.firstWeekContainsDate ?? ((_b = (_a = defaultOptions2.locale) == null ? void 0 : _a.options) == null ? void 0 : _b.firstWeekContainsDate) ?? 1;
	const weekStartsOn = defaultOptions2.weekStartsOn ?? ((_d = (_c = defaultOptions2.locale) == null ? void 0 : _c.options) == null ? void 0 : _d.weekStartsOn) ?? 0;
	const originalDate = toDate(date);
	if (!isValid(originalDate)) throw new RangeError("Invalid time value");
	let parts = formatStr.match(longFormattingTokensRegExp).map((substring) => {
		const firstCharacter = substring[0];
		if (firstCharacter === "p" || firstCharacter === "P") {
			const longFormatter = longFormatters[firstCharacter];
			return longFormatter(substring, locale.formatLong);
		}
		return substring;
	}).join("").match(formattingTokensRegExp).map((substring) => {
		if (substring === "''") return {
			isToken: false,
			value: "'"
		};
		const firstCharacter = substring[0];
		if (firstCharacter === "'") return {
			isToken: false,
			value: cleanEscapedString(substring)
		};
		if (formatters[firstCharacter]) return {
			isToken: true,
			value: substring
		};
		if (firstCharacter.match(unescapedLatinCharacterRegExp)) throw new RangeError("Format string contains an unescaped latin alphabet character `" + firstCharacter + "`");
		return {
			isToken: false,
			value: substring
		};
	});
	if (locale.localize.preprocessor) parts = locale.localize.preprocessor(originalDate, parts);
	const formatterOptions = {
		firstWeekContainsDate,
		weekStartsOn,
		locale
	};
	return parts.map((part) => {
		if (!part.isToken) return part.value;
		const token = part.value;
		if (isProtectedWeekYearToken(token) || isProtectedDayOfYearToken(token)) warnOrThrowProtectedError(token, formatStr, String(date));
		const formatter = formatters[token[0]];
		return formatter(originalDate, token, locale.localize, formatterOptions);
	}).join("");
}
function cleanEscapedString(input) {
	const matched = input.match(escapedStringRegExp);
	if (!matched) return input;
	return matched[1].replace(doubleQuoteRegExp, "'");
}
function getDaysInMonth(date) {
	const _date = toDate(date);
	const year = _date.getFullYear();
	const monthIndex = _date.getMonth();
	const lastDayOfMonth2 = constructFrom(date, 0);
	lastDayOfMonth2.setFullYear(year, monthIndex + 1, 0);
	lastDayOfMonth2.setHours(0, 0, 0, 0);
	return lastDayOfMonth2.getDate();
}
function lastDayOfMonth(date) {
	const _date = toDate(date);
	const month = _date.getMonth();
	_date.setFullYear(_date.getFullYear(), month + 1, 0);
	_date.setHours(0, 0, 0, 0);
	return _date;
}
function isAfter(date, dateToCompare) {
	const _date = toDate(date);
	const _dateToCompare = toDate(dateToCompare);
	return _date.getTime() > _dateToCompare.getTime();
}
function isBefore(date, dateToCompare) {
	const _date = toDate(date);
	const _dateToCompare = toDate(dateToCompare);
	return +_date < +_dateToCompare;
}
function isSameMonth(dateLeft, dateRight) {
	const _dateLeft = toDate(dateLeft);
	const _dateRight = toDate(dateRight);
	return _dateLeft.getFullYear() === _dateRight.getFullYear() && _dateLeft.getMonth() === _dateRight.getMonth();
}
function parseISO(argument, options) {
	const additionalDigits = 2;
	const dateStrings = splitDateString(argument);
	let date;
	if (dateStrings.date) {
		const parseYearResult = parseYear(dateStrings.date, additionalDigits);
		date = parseDate(parseYearResult.restDateString, parseYearResult.year);
	}
	if (!date || isNaN(date.getTime())) return /* @__PURE__ */ new Date(NaN);
	const timestamp = date.getTime();
	let time = 0;
	let offset;
	if (dateStrings.time) {
		time = parseTime(dateStrings.time);
		if (isNaN(time)) return /* @__PURE__ */ new Date(NaN);
	}
	if (dateStrings.timezone) {
		offset = parseTimezone(dateStrings.timezone);
		if (isNaN(offset)) return /* @__PURE__ */ new Date(NaN);
	} else {
		const dirtyDate = new Date(timestamp + time);
		const result = /* @__PURE__ */ new Date(0);
		result.setFullYear(dirtyDate.getUTCFullYear(), dirtyDate.getUTCMonth(), dirtyDate.getUTCDate());
		result.setHours(dirtyDate.getUTCHours(), dirtyDate.getUTCMinutes(), dirtyDate.getUTCSeconds(), dirtyDate.getUTCMilliseconds());
		return result;
	}
	return new Date(timestamp + time + offset);
}
var patterns = {
	dateTimeDelimiter: /[T ]/,
	timeZoneDelimiter: /[Z ]/i,
	timezone: /([Z+-].*)$/
};
var dateRegex = /^-?(?:(\d{3})|(\d{2})(?:-?(\d{2}))?|W(\d{2})(?:-?(\d{1}))?|)$/;
var timeRegex = /^(\d{2}(?:[.,]\d*)?)(?::?(\d{2}(?:[.,]\d*)?))?(?::?(\d{2}(?:[.,]\d*)?))?$/;
var timezoneRegex = /^([+-])(\d{2})(?::?(\d{2}))?$/;
function splitDateString(dateString) {
	const dateStrings = {};
	const array = dateString.split(patterns.dateTimeDelimiter);
	let timeString;
	if (array.length > 2) return dateStrings;
	if (/:/.test(array[0])) timeString = array[0];
	else {
		dateStrings.date = array[0];
		timeString = array[1];
		if (patterns.timeZoneDelimiter.test(dateStrings.date)) {
			dateStrings.date = dateString.split(patterns.timeZoneDelimiter)[0];
			timeString = dateString.substr(dateStrings.date.length, dateString.length);
		}
	}
	if (timeString) {
		const token = patterns.timezone.exec(timeString);
		if (token) {
			dateStrings.time = timeString.replace(token[1], "");
			dateStrings.timezone = token[1];
		} else dateStrings.time = timeString;
	}
	return dateStrings;
}
function parseYear(dateString, additionalDigits) {
	const regex = new RegExp("^(?:(\\d{4}|[+-]\\d{" + (4 + additionalDigits) + "})|(\\d{2}|[+-]\\d{" + (2 + additionalDigits) + "})$)");
	const captures = dateString.match(regex);
	if (!captures) return {
		year: NaN,
		restDateString: ""
	};
	const year = captures[1] ? parseInt(captures[1]) : null;
	const century = captures[2] ? parseInt(captures[2]) : null;
	return {
		year: century === null ? year : century * 100,
		restDateString: dateString.slice((captures[1] || captures[2]).length)
	};
}
function parseDate(dateString, year) {
	if (year === null) return /* @__PURE__ */ new Date(NaN);
	const captures = dateString.match(dateRegex);
	if (!captures) return /* @__PURE__ */ new Date(NaN);
	const isWeekDate = !!captures[4];
	const dayOfYear = parseDateUnit(captures[1]);
	const month = parseDateUnit(captures[2]) - 1;
	const day = parseDateUnit(captures[3]);
	const week = parseDateUnit(captures[4]);
	const dayOfWeek = parseDateUnit(captures[5]) - 1;
	if (isWeekDate) {
		if (!validateWeekDate(year, week, dayOfWeek)) return /* @__PURE__ */ new Date(NaN);
		return dayOfISOWeekYear(year, week, dayOfWeek);
	} else {
		const date = /* @__PURE__ */ new Date(0);
		if (!validateDate(year, month, day) || !validateDayOfYearDate(year, dayOfYear)) return /* @__PURE__ */ new Date(NaN);
		date.setUTCFullYear(year, month, Math.max(dayOfYear, day));
		return date;
	}
}
function parseDateUnit(value) {
	return value ? parseInt(value) : 1;
}
function parseTime(timeString) {
	const captures = timeString.match(timeRegex);
	if (!captures) return NaN;
	const hours = parseTimeUnit(captures[1]);
	const minutes = parseTimeUnit(captures[2]);
	const seconds = parseTimeUnit(captures[3]);
	if (!validateTime(hours, minutes, seconds)) return NaN;
	return hours * millisecondsInHour + minutes * millisecondsInMinute + seconds * 1e3;
}
function parseTimeUnit(value) {
	return value && parseFloat(value.replace(",", ".")) || 0;
}
function parseTimezone(timezoneString) {
	if (timezoneString === "Z") return 0;
	const captures = timezoneString.match(timezoneRegex);
	if (!captures) return 0;
	const sign = captures[1] === "+" ? -1 : 1;
	const hours = parseInt(captures[2]);
	const minutes = captures[3] && parseInt(captures[3]) || 0;
	if (!validateTimezone(hours, minutes)) return NaN;
	return sign * (hours * millisecondsInHour + minutes * millisecondsInMinute);
}
function dayOfISOWeekYear(isoWeekYear, week, day) {
	const date = /* @__PURE__ */ new Date(0);
	date.setUTCFullYear(isoWeekYear, 0, 4);
	const fourthOfJanuaryDay = date.getUTCDay() || 7;
	const diff = (week - 1) * 7 + day + 1 - fourthOfJanuaryDay;
	date.setUTCDate(date.getUTCDate() + diff);
	return date;
}
var daysInMonths = [
	31,
	null,
	31,
	30,
	31,
	30,
	31,
	31,
	30,
	31,
	30,
	31
];
function isLeapYearIndex(year) {
	return year % 400 === 0 || year % 4 === 0 && year % 100 !== 0;
}
function validateDate(year, month, date) {
	return month >= 0 && month <= 11 && date >= 1 && date <= (daysInMonths[month] || (isLeapYearIndex(year) ? 29 : 28));
}
function validateDayOfYearDate(year, dayOfYear) {
	return dayOfYear >= 1 && dayOfYear <= (isLeapYearIndex(year) ? 366 : 365);
}
function validateWeekDate(_year, week, day) {
	return week >= 1 && week <= 53 && day >= 0 && day <= 6;
}
function validateTime(hours, minutes, seconds) {
	if (hours === 24) return minutes === 0 && seconds === 0;
	return seconds >= 0 && seconds < 60 && minutes >= 0 && minutes < 60 && hours >= 0 && hours < 25;
}
function validateTimezone(_hours, minutes) {
	return minutes >= 0 && minutes <= 59;
}
var CalendarDate = class CalendarDate {
	constructor(value) {
		__publicField(this, "_dateNums");
		if (value || value === 0) this._dateNums = CalendarDate.parse(value);
		else this._dateNums = CalendarDate.parse(/* @__PURE__ */ new Date());
	}
	static parse(value) {
		if (typeof value === "string") {
			value = value.split("T")[0];
			return value.split("-").map((v) => +v);
		} else if (value instanceof Date) return [
			value.getFullYear(),
			value.getMonth() + 1,
			value.getDate()
		];
		else if (value === 0) return [
			0,
			0,
			0
		];
		else return [
			value.year,
			value.month,
			value.day
		];
	}
	static init() {
		return new CalendarDate(0);
	}
	get date() {
		return new Date(this._dateNums[0], this._dateNums[1] - 1, this._dateNums[2]);
	}
	get year() {
		return this._dateNums[0];
	}
	get month() {
		return this._dateNums[1];
	}
	get day() {
		return this._dateNums[2];
	}
	get dayOfWeek() {
		return this.date.getDay();
	}
	get daysInMonth() {
		return getDaysInMonth(this.date);
	}
	get firstDayOfMonth() {
		return new CalendarDate({
			year: this.year,
			month: this.month,
			day: 1
		});
	}
	get lastDayOfMonth() {
		return new CalendarDate(lastDayOfMonth(this.date));
	}
	get previousDay() {
		return this.clone().addDays(-1);
	}
	get nextDay() {
		return this.clone().addDays(1);
	}
	get previousWeek() {
		return this.clone().addDays(-7);
	}
	get nextWeek() {
		return this.clone().addDays(7);
	}
	get previousMonth() {
		return this.clone().addMonths(-1);
	}
	get nextMonth() {
		return this.clone().addMonths(1);
	}
	clone() {
		return new CalendarDate(this.toString());
	}
	setYear(val) {
		this._dateNums[0] = val;
	}
	setMonth(val) {
		this._dateNums[1] = val;
	}
	setDay(val) {
		this._dateNums[2] = val;
		return this;
	}
	addYears(count) {
		this._dateNums[0] += count;
		return this;
	}
	addMonths(count) {
		this._dateNums = CalendarDate.parse(addMonths(this.date, count));
		return this;
	}
	addDays(count) {
		this._dateNums = CalendarDate.parse(addDays(this.date, count));
		return this;
	}
	isSameDay(cmp) {
		return isSameDay(this.date, cmp.date);
	}
	isSameMonth(value) {
		return isSameMonth(this.date, value.date);
	}
	isBefore(cmp) {
		return isBefore(this.date, cmp.date);
	}
	isAfter(cmp) {
		return isAfter(this.date, cmp.date);
	}
	isZero() {
		return this._dateNums[0] === 0 && this._dateNums[1] === 0 && this._dateNums[2] === 0;
	}
	isValid() {
		const comparisonDate = new Date(this.toString());
		if (isNaN(comparisonDate.getTime()) || this.toString() !== comparisonDate.toISOString().split("T")[0]) return false;
		return true;
	}
	format(tmpl) {
		if (this.isZero()) return "";
		return format(this.date, tmpl);
	}
	toString() {
		if (this.isZero()) return "";
		return this._dateNums.map((num) => `${num}`.length < 2 ? `0${num}` : `${num}`).join("-");
	}
};
function GoabDatePicker({ value, error, min, max, disabled, relative, onChange, ...rest }) {
	const ref = (0, import_react.useRef)(null);
	const _props = transformProps(rest, lowercase);
	(0, import_react.useEffect)(() => {
		if (value && typeof value !== "string") console.warn("Using a `Date` type for value is deprecated. Instead use a string of the format `yyyy-mm-dd`");
	}, []);
	(0, import_react.useEffect)(() => {
		if (!ref.current) return;
		const current = ref.current;
		const handleChange = (e) => {
			const detail = e.detail;
			onChange?.({
				...detail,
				event: e
			});
		};
		if (onChange) current.addEventListener("_change", handleChange);
		return () => {
			if (onChange) current.removeEventListener("_change", handleChange);
		};
	}, [onChange]);
	const formatValue = (val) => {
		if (!val) return "";
		if (val instanceof Date) return new CalendarDate(val).toString();
		return val;
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-date-picker", {
		ref,
		value: formatValue(value) || void 0,
		error: error ? "true" : void 0,
		disabled: disabled ? "true" : void 0,
		min: formatValue(min) || void 0,
		max: formatValue(max) || void 0,
		relative: relative ? "true" : void 0,
		version: "2",
		..._props
	});
}
function GoabDetails({ open, children, ...rest }) {
	const _props = transformProps(rest, lowercase);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-details", {
		open: open ? "true" : void 0,
		..._props,
		children
	});
}
function GoabDivider(props) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-divider", {
		mt: props.mt,
		mr: props.mr,
		mb: props.mb,
		ml: props.ml,
		testid: props.testId
	});
}
function GoabDrawer({ position, open, heading, maxSize, testId, actions, children, onClose }) {
	const el = (0, import_react.useRef)(null);
	(0, import_react.useEffect)(() => {
		if (!(el == null ? void 0 : el.current) || !onClose) return;
		const current = el.current;
		const listener = (e) => {
			if (e.target !== current) return;
			onClose();
		};
		current.addEventListener("_close", listener);
		return () => {
			current.removeEventListener("_close", listener);
		};
	}, [el, onClose]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("goa-drawer", {
		ref: el,
		position,
		open: open ? true : void 0,
		heading: typeof heading === "string" ? heading : void 0,
		maxsize: maxSize,
		testid: testId,
		version: "2",
		children: [
			heading && typeof heading !== "string" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				slot: "heading",
				children: heading
			}),
			actions && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				slot: "actions",
				children: actions
			}),
			children
		]
	});
}
function stringify(value) {
	if (typeof value === "undefined") return "";
	if (typeof value === "string") return value;
	return JSON.stringify(value);
}
function GoabDropdown({ value, onChange, disabled, error, filterable, multiselect, native, relative, children, size = "default", ...rest }) {
	const el = (0, import_react.useRef)(null);
	const _props = transformProps({
		size,
		...rest
	}, lowercase);
	(0, import_react.useEffect)(() => {
		if (!el.current) return;
		const current = el.current;
		const handler = (e) => {
			const detail = e.detail;
			onChange?.({
				...detail,
				event: e
			});
		};
		if (onChange) current.addEventListener("_change", handler);
		return () => {
			if (onChange) current.removeEventListener("_change", handler);
		};
	}, [el, onChange]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-dropdown", {
		ref: el,
		value: stringify(value),
		disabled: disabled ? "true" : void 0,
		error: error ? "true" : void 0,
		filterable: filterable ? "true" : void 0,
		multiselect: multiselect ? "true" : void 0,
		native: native ? "true" : void 0,
		relative: relative ? "true" : void 0,
		..._props,
		version: "2",
		children
	});
}
function GoabDropdownOption(props) {
	(0, import_react.useEffect)(() => {
		console.warn("GoabDropdownOption is deprecated. Please use GoabDropdownItem");
	}, []);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GoabDropdownItem, { ...props });
}
function GoabDropdownItem({ value, label, filter, name, mountType = "append" }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-dropdown-item", {
		value,
		label,
		filter,
		name,
		mount: mountType
	});
}
function GoabFileUploadCard({ onDelete, onCancel, filename, ...rest }) {
	const el = (0, import_react.useRef)(null);
	const _props = transformProps({
		filename,
		...rest
	}, lowercase);
	(0, import_react.useEffect)(() => {
		if (!el.current) return;
		const current = el.current;
		const deleteHandler = (event) => onDelete == null ? void 0 : onDelete({
			filename,
			event
		});
		const cancelHandler = (event) => onCancel == null ? void 0 : onCancel({
			filename,
			event
		});
		current.addEventListener("_delete", deleteHandler);
		current.addEventListener("_cancel", cancelHandler);
		return () => {
			current.removeEventListener("_delete", deleteHandler);
			current.removeEventListener("_cancel", cancelHandler);
		};
	}, [
		el,
		onDelete,
		onCancel,
		filename
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-file-upload-card", {
		ref: el,
		..._props,
		version: "2"
	});
}
function GoabFileUploadInput({ onSelectFile, ...rest }) {
	const el = (0, import_react.useRef)(null);
	const _props = transformProps(rest, lowercase);
	(0, import_react.useEffect)(() => {
		if (!el.current) return;
		const current = el.current;
		const handler = (e) => {
			const detail = e.detail;
			onSelectFile({
				...detail,
				event: e
			});
		};
		current.addEventListener("_selectFile", handler);
		return () => {
			current.removeEventListener("_selectFile", handler);
		};
	}, [el, onSelectFile]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-file-upload-input", {
		ref: el,
		..._props,
		version: "2"
	});
}
function GoabAppFooter({ children, ...rest }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-app-footer", {
		...transformProps(rest, lowercase),
		version: "2",
		children
	});
}
function GoabAppFooterMetaSection({ children, ...rest }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-app-footer-meta-section", {
		slot: "meta",
		...transformProps(rest, lowercase),
		children
	});
}
function GoabAppFooterNavSection({ children, ...rest }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-app-footer-nav-section", {
		slot: "nav",
		...transformProps(rest, lowercase),
		children
	});
}
function GoabFieldset({ onContinue, children, ...rest }) {
	const ref = (0, import_react.useRef)(null);
	const _props = transformProps(rest, kebab);
	(0, import_react.useEffect)(() => {
		if (!ref.current) return;
		const current = ref.current;
		const continueListener = (e) => {
			const event = e.detail;
			return onContinue == null ? void 0 : onContinue(event);
		};
		if (onContinue) current.addEventListener("_continue", continueListener);
		return () => {
			if (onContinue) current.removeEventListener("_continue", continueListener);
		};
	}, [ref, onContinue]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-fieldset", {
		ref,
		..._props,
		children
	});
}
function GoabPublicFormPage({ onContinue, children, ...rest }) {
	const ref = (0, import_react.useRef)(null);
	const _props = transformProps(rest, kebab);
	(0, import_react.useEffect)(() => {
		if (!ref.current) return;
		const current = ref.current;
		const continueListener = (e) => {
			onContinue?.(e);
		};
		if (onContinue) current.addEventListener("_continue", continueListener);
		return () => {
			if (onContinue) current.removeEventListener("_continue", continueListener);
		};
	}, [ref, onContinue]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-public-form-page", {
		ref,
		..._props,
		children
	});
}
function GoabPublicFormSummary({ heading = "", ...rest }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-public-form-summary", { ...transformProps({
		heading,
		...rest
	}, lowercase) });
}
function GoabPublicForm({ onInit, onComplete, onStateChange, children, ...rest }) {
	const ref = (0, import_react.useRef)(null);
	const initialized = (0, import_react.useRef)(false);
	const _props = transformProps(rest, lowercase);
	(0, import_react.useLayoutEffect)(() => {
		if (!ref.current) return;
		const current = ref.current;
		const initListener = (e) => {
			onInit?.(e);
		};
		if (onInit && !initialized.current) current.addEventListener("_init", initListener);
		const completeListener = (e) => {
			const detail = e.detail;
			onComplete?.(detail);
		};
		const stateChangeListener = (e) => {
			const detail = e.detail;
			onStateChange?.(detail.data);
		};
		if (onComplete) current.addEventListener("_complete", completeListener);
		if (onStateChange) current.addEventListener("_stateChange", stateChangeListener);
		return () => {
			if (onInit) current.removeEventListener("_init", initListener);
			if (onComplete) current.removeEventListener("_complete", completeListener);
			if (onStateChange) current.removeEventListener("_stateChange", stateChangeListener);
		};
	}, [
		onInit,
		onComplete,
		onStateChange
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-public-form", {
		ref,
		..._props,
		children
	});
}
function GoabPublicSubform({ id = "", name = "", continueMsg = "", onInit, onStateChange, children, ...rest }) {
	const ref = (0, import_react.useRef)(null);
	const _props = transformProps({
		id,
		name,
		"continue-msg": continueMsg,
		...rest
	}, kebab);
	(0, import_react.useEffect)(() => {
		if (!ref.current) return;
		const current = ref.current;
		const initListener = (e) => {
			onInit?.(e);
		};
		const stateChangeListener = (e) => {
			onStateChange?.(e);
		};
		if (onInit) current.addEventListener("_init", initListener);
		if (onStateChange) current.addEventListener("_stateChange", stateChangeListener);
		return () => {
			if (onInit) current.removeEventListener("_init", initListener);
			if (onStateChange) current.removeEventListener("_stateChange", stateChangeListener);
		};
	}, [
		ref,
		onInit,
		onStateChange
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-public-subform", {
		ref,
		..._props,
		children
	});
}
function GoabPublicSubformIndex({ heading = "", sectionTitle = "", actionButtonText = "", buttonVisibility = "hidden", children, ...rest }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-public-subform-index", {
		slot: "subform-index",
		...transformProps({
			heading,
			"section-title": sectionTitle,
			"action-button-text": actionButtonText,
			"button-visibility": buttonVisibility,
			...rest
		}, kebab),
		children
	});
}
function GoabPublicFormTask({ status = "cannot-start", children, ...rest }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-public-form-task", {
		...transformProps({
			status,
			...rest
		}, lowercase),
		children
	});
}
function GoabPublicFormTaskList({ children, ...rest }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-public-form-task-list", {
		...transformProps(rest, lowercase),
		children
	});
}
function GoabFormItem({ error, helpText, publicFormSummaryOrder, children, type = "", ...rest }) {
	const _props = transformProps({
		type,
		...rest
	}, lowercase);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("goa-form-item", {
		error: typeof error === "string" ? error : void 0,
		helptext: typeof helpText === "string" ? helpText : void 0,
		"public-form-summary-order": publicFormSummaryOrder,
		..._props,
		version: "2",
		children: [
			error && typeof error !== "string" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				slot: "error",
				children: error
			}),
			helpText && typeof helpText !== "string" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				slot: "helptext",
				children: helpText
			}),
			children
		]
	});
}
function GoabFormStep(props) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-form-step", {
		text: props.text,
		status: props.status
	});
}
function GoabFormStepper({ testId, step, mt, mb, ml, mr, onChange, children }) {
	const ref = (0, import_react.useRef)(null);
	(0, import_react.useEffect)(() => {
		if (!ref.current) return;
		const current = ref.current;
		const changeListener = (e) => {
			const detail = e.detail;
			onChange?.(detail);
		};
		current.addEventListener("_change", changeListener);
		return () => {
			current.removeEventListener("_change", changeListener);
		};
	}, [ref, onChange]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-form-stepper", {
		ref,
		testid: testId,
		step,
		mt,
		mr,
		mb,
		ml,
		children
	});
}
function GoabGrid({ gap, minChildWidth, mt, mr, mb, ml, testId, children }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-grid", {
		gap,
		minchildwidth: minChildWidth,
		mt,
		mr,
		mb,
		ml,
		testid: testId,
		children
	});
}
function GoabHeroBanner({ heading, backgroundUrl, minHeight, maxContentWidth, backgroundColor, textColor, children, actions, testId }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("goa-hero-banner", {
		heading,
		backgroundurl: backgroundUrl,
		minheight: minHeight,
		maxcontentwidth: maxContentWidth,
		backgroundcolor: backgroundColor,
		textcolor: textColor,
		testid: testId,
		children: [children, actions && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			slot: "actions",
			children: actions
		})]
	});
}
function GoabHeroBannerActions({ children }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		slot: "actions",
		children
	});
}
function GoabIconButton({ variant = "color", size = "medium", theme = "outline", disabled, onClick, actionArgs, actionArg, children, ...rest }) {
	const ref = (0, import_react.useRef)(null);
	const _props = transformProps({
		variant,
		size,
		theme,
		...rest
	}, lowercase);
	(0, import_react.useEffect)(() => {
		if (!ref.current) return;
		if (!onClick) return;
		const current = ref.current;
		const listener = () => {
			onClick?.();
		};
		current.addEventListener("_click", listener);
		return () => {
			current.removeEventListener("_click", listener);
		};
	}, [ref, onClick]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-icon-button", {
		ref,
		disabled: disabled ? "true" : void 0,
		"action-arg": actionArg,
		"action-args": JSON.stringify(actionArgs),
		..._props,
		children
	});
}
function GoabIcon({ inverted, ...rest }) {
	const _props = transformProps(rest, lowercase);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-icon", {
		inverted: typeof inverted === "boolean" ? inverted ? "true" : void 0 : inverted,
		..._props
	});
}
function GoabInput({ variant = "goa", textAlign = "left", size = "default", focused, disabled, readonly, error, leadingContent, trailingContent, onTrailingIconClick, onChange, onFocus, onBlur, onKeyPress, ...rest }) {
	const ref = (0, import_react.useRef)(null);
	const _props = transformProps({
		variant,
		textalign: textAlign,
		size,
		...rest
	}, lowercase);
	(0, import_react.useEffect)(() => {
		if (!ref.current) return;
		const current = ref.current;
		const changeListener = (e) => {
			const detail = e.detail;
			onChange?.({
				...detail,
				event: e
			});
		};
		const clickListener = () => {
			onTrailingIconClick?.();
		};
		const focusListener = (e) => {
			const detail = e.detail;
			onFocus?.({
				...detail,
				event: e
			});
		};
		const blurListener = (e) => {
			const detail = e.detail;
			onBlur?.({
				...detail,
				event: e
			});
		};
		const keypressListener = (e) => {
			const detail = e.detail;
			onKeyPress?.({
				...detail,
				event: e
			});
		};
		current.addEventListener("_change", changeListener);
		current.addEventListener("_trailingIconClick", clickListener);
		current.addEventListener("_focus", focusListener);
		current.addEventListener("_blur", blurListener);
		current.addEventListener("_keyPress", keypressListener);
		return () => {
			current.removeEventListener("_change", changeListener);
			current.removeEventListener("_trailingIconClick", clickListener);
			current.removeEventListener("_focus", focusListener);
			current.removeEventListener("_blur", blurListener);
			current.removeEventListener("_keyPress", keypressListener);
		};
	}, [
		ref,
		onChange,
		onTrailingIconClick,
		onFocus,
		onBlur,
		onKeyPress
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("goa-input", {
		ref,
		..._props,
		focused: focused ? "true" : void 0,
		disabled: disabled ? "true" : void 0,
		readonly: readonly ? "true" : void 0,
		error: error ? "true" : void 0,
		handletrailingiconclick: onTrailingIconClick ? "true" : "false",
		version: "2",
		children: [leadingContent && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			slot: "leadingContent",
			children: leadingContent
		}), trailingContent && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			slot: "trailingContent",
			children: trailingContent
		})]
	});
}
var onDateChangeHandler = (onChange) => {
	return ({ name, value, event }) => {
		if (!value) {
			onChange?.({
				name,
				value: "",
				event
			});
			return;
		}
		if (typeof value === "string" && isValid(new Date(value))) {
			onChange?.({
				name,
				value: parseISO(value),
				event
			});
			return;
		}
		if (isValid(value)) {
			onChange?.({
				name,
				value,
				event
			});
			return;
		}
	};
};
var onTimeChangeHandler = (onChange) => {
	return ({ name, value, event }) => {
		if (!value) {
			onChange?.({
				name,
				value: "",
				event
			});
			return;
		}
		onChange?.({
			name,
			value,
			event
		});
	};
};
function toString(value, tmpl = "yyyy-MM-dd") {
	if (!value) return "";
	if (typeof value === "string") return format(parseISO(value), tmpl);
	if (value.toISOString() === (/* @__PURE__ */ new Date(0)).toISOString()) return "";
	return format(value, tmpl);
}
function GoabInputText(props) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GoabInput, {
		...props,
		type: "text"
	});
}
function GoabInputPassword(props) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GoabInput, {
		...props,
		type: "password"
	});
}
function GoabInputDate({ value, min = "", max = "", ...props }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GoabInput, {
		...props,
		type: "date",
		onChange: onDateChangeHandler(props.onChange),
		min: toString(min),
		max: toString(max),
		value: toString(value)
	});
}
function GoabInputTime({ value, min = "", max = "", ...props }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GoabInput, {
		...props,
		onChange: onTimeChangeHandler(props.onChange),
		value,
		type: "time"
	});
}
function GoabInputDateTime({ value, min = "", max = "", ...props }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GoabInput, {
		...props,
		onChange: onDateChangeHandler(props.onChange),
		value: toString(value, "yyyy-MM-dd'T'HH:mm"),
		type: "datetime-local"
	});
}
function GoabInputEmail(props) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GoabInput, {
		...props,
		type: "email"
	});
}
function GoabInputSearch(props) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GoabInput, {
		...props,
		type: "search",
		trailingIcon: "search"
	});
}
function GoabInputUrl(props) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GoabInput, {
		...props,
		type: "url"
	});
}
function GoabInputTel(props) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GoabInput, {
		...props,
		type: "tel"
	});
}
function GoabInputFile(props) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", {
		id: props.id,
		name: props.name,
		type: "file",
		onChange: (e) => {
			var _a;
			return (_a = props.onChange) == null ? void 0 : _a.call(props, {
				name: e.target.name,
				value: e.target.value,
				event: e.nativeEvent
			});
		},
		style: { backgroundColor: "revert" }
	});
}
function GoabInputMonth(props) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GoabInput, {
		...props,
		type: "month"
	});
}
function GoabInputNumber({ min = Number.MIN_VALUE, max = Number.MAX_VALUE, value, textAlign = "right", ...props }) {
	const onNumberChange = ({ name, value: value2, event }) => {
		var _a;
		(_a = props.onChange) == null || _a.call(props, {
			name,
			value: parseFloat(value2),
			event
		});
	};
	const onFocus = ({ name, value: value2, event }) => {
		var _a;
		(_a = props.onFocus) == null || _a.call(props, {
			name,
			value: parseFloat(value2),
			event
		});
	};
	const onBlur = ({ name, value: value2, event }) => {
		var _a;
		(_a = props.onBlur) == null || _a.call(props, {
			name,
			value: parseFloat(value2),
			event
		});
	};
	const onKeyPress = ({ name, value: value2, key, event }) => {
		var _a;
		(_a = props.onKeyPress) == null || _a.call(props, {
			name,
			value: parseFloat(value2),
			key: parseInt(key),
			event
		});
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GoabInput, {
		...props,
		onChange: onNumberChange,
		min: min == null ? void 0 : min.toString(),
		max: max == null ? void 0 : max.toString(),
		value: value == null ? void 0 : value.toString(),
		onFocus,
		onBlur,
		type: "number",
		onKeyPress,
		textAlign
	});
}
function GoabInputRange(props) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GoabInput, {
		...props,
		type: "range"
	});
}
var GoabLinearProgress = ({ progress, percentVisibility, ariaLabel, ariaLabelledBy, testId }) => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-linear-progress", {
		progress,
		"percent-visibility": percentVisibility,
		"aria-label": ariaLabel,
		"aria-labelledby": ariaLabelledBy,
		testid: testId
	});
};
function GoabLink({ actionArgs, actionArg, color = "interactive", size = "medium", children, ...rest }) {
	const _props = transformProps({
		color,
		size,
		...rest
	}, lowercase);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-link", {
		"action-arg": actionArg,
		"action-args": JSON.stringify(actionArgs),
		..._props,
		children
	});
}
function GoALinkButton({ type = "primary", children, ...rest }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-link-button", {
		...transformProps({
			type,
			...rest
		}, lowercase),
		children
	});
}
function GoabMenuButton({ type = "primary", testId, onAction, children, ...rest }) {
	const el = (0, import_react.useRef)(null);
	const _props = transformProps({
		type,
		testid: testId,
		...rest
	}, kebab);
	(0, import_react.useEffect)(() => {
		if (!el.current) return;
		if (!onAction) return;
		const current = el.current;
		const listener = (e) => {
			const detail = e.detail;
			onAction?.(detail);
		};
		current.addEventListener("_action", listener);
		return () => {
			current.removeEventListener("_action", listener);
		};
	}, [el, onAction]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-menu-button", {
		..._props,
		version: "2",
		ref: el,
		children
	});
}
function GoabMenuAction(props) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-menu-action", { ...transformProps(props, lowercase) });
}
function GoabMicrositeHeader({ type, version, feedbackUrl, maxContentWidth, feedbackUrlTarget, headerUrlTarget, testId, onFeedbackClick }) {
	const el = (0, import_react.useRef)(null);
	(0, import_react.useEffect)(() => {
		if (!el.current) return;
		if (!onFeedbackClick) return;
		const current = el.current;
		const listener = () => {
			onFeedbackClick();
		};
		current.addEventListener("_feedbackClick", listener);
		return () => {
			current.removeEventListener("_feedbackClick", listener);
		};
	}, [el, onFeedbackClick]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-microsite-header", {
		ref: el,
		type,
		version: typeof version === "string" ? version : void 0,
		feedbackurl: feedbackUrl,
		testid: testId,
		maxcontentwidth: maxContentWidth,
		feedbackurltarget: feedbackUrlTarget,
		headerurltarget: headerUrlTarget,
		hasfeedbackhandler: onFeedbackClick ? "true" : "false",
		children: version && typeof version !== "string" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			slot: "version",
			children: version
		})
	});
}
function GoabModal({ heading, children, maxWidth, open, actions, transition, calloutVariant, onClose, testId }) {
	const el = (0, import_react.useRef)(null);
	(0, import_react.useEffect)(() => {
		if (!el.current) return;
		const current = el.current;
		const listener = (e) => {
			if (e.target !== current) return;
			onClose?.();
		};
		current.addEventListener("_close", listener);
		return () => {
			current.removeEventListener("_close", listener);
		};
	}, [el, onClose]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("goa-modal", {
		ref: el,
		open: open ? "true" : void 0,
		closable: onClose ? "true" : "false",
		heading: typeof heading === "string" ? heading : void 0,
		maxwidth: maxWidth,
		transition,
		calloutvariant: calloutVariant,
		testid: testId,
		version: "2",
		children: [
			heading && typeof heading !== "string" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				slot: "heading",
				children: heading
			}),
			actions && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				slot: "actions",
				children: actions
			}),
			children
		]
	});
}
var GoabNotification = ({ type = "information", emphasis = "high", compact, ariaLive, maxContentWidth, children, testId, onDismiss }) => {
	const el = (0, import_react.useRef)(null);
	(0, import_react.useEffect)(() => {
		if (!el.current) return;
		const current = el.current;
		const listener = () => {
			onDismiss?.();
		};
		current.addEventListener("_dismiss", listener);
		return () => {
			current.removeEventListener("_dismiss", listener);
		};
	}, [el, onDismiss]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-notification", {
		ref: el,
		type,
		testid: testId,
		maxcontentwidth: maxContentWidth,
		arialive: ariaLive,
		emphasis,
		compact: compact ? "true" : void 0,
		version: "2",
		children
	});
};
function GoabOneColumnLayout(props) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-one-column-layout", { children: props.children });
}
function GoabPageBlock(props) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-page-block", {
		width: props.width,
		testid: props.testId,
		children: props.children
	});
}
function GoabPages(props) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-pages", {
		current: props.current,
		ml: props.ml,
		mr: props.mr,
		mt: props.mt,
		mb: props.mb,
		children: props.children
	});
}
function GoabPagination({ onChange, ...props }) {
	const ref = (0, import_react.useRef)(null);
	(0, import_react.useEffect)(() => {
		if (!ref.current) return;
		const current = ref.current;
		const changeListener = (e) => {
			const detail = e.detail;
			onChange(detail);
		};
		current.addEventListener("_change", changeListener);
		return () => {
			current.removeEventListener("_change", changeListener);
		};
	}, [ref, onChange]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-pagination", {
		ref,
		itemcount: props.itemCount,
		perpagecount: props.perPageCount,
		pagenumber: props.pageNumber,
		variant: props.variant,
		mt: props.mt,
		mb: props.mb,
		ml: props.ml,
		mr: props.mr,
		testid: props.testId,
		version: "2"
	});
}
function GoabPopover({ target, padded, relative, children, ...rest }) {
	const _props = transformProps(rest, lowercase);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("goa-popover", {
		padded: typeof padded === "undefined" ? void 0 : padded ? "true" : "false",
		relative: relative ? "true" : void 0,
		..._props,
		children: [children, target && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			slot: "target",
			children: target
		})]
	});
}
function GoabPushDrawer({ open, heading, width, testId, actions, children, onClose }) {
	const el = (0, import_react.useRef)(null);
	(0, import_react.useEffect)(() => {
		if (!(el == null ? void 0 : el.current) || !onClose) return;
		const current = el.current;
		const listener = (e) => {
			if (e.target !== current) return;
			onClose();
		};
		current.addEventListener("_close", listener);
		return () => {
			current.removeEventListener("_close", listener);
		};
	}, [el, onClose]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("goa-push-drawer", {
		ref: el,
		open: open ? true : void 0,
		heading: typeof heading === "string" ? heading : void 0,
		width,
		testid: testId,
		version: "2",
		children: [
			heading && typeof heading !== "string" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				slot: "heading",
				children: heading
			}),
			actions && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				slot: "actions",
				children: actions
			}),
			children
		]
	});
}
function GoabRadioItem({ name, label, value, description, reveal, revealAriaLabel, maxWidth, disabled, checked, error, compact, ariaLabel, children, mt, mr, mb, ml }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("goa-radio-item", {
		name,
		label,
		value,
		description: typeof description === "string" ? description : void 0,
		maxwidth: maxWidth,
		error: error ? "true" : void 0,
		disabled: disabled ? "true" : void 0,
		checked: checked ? "true" : void 0,
		compact: compact ? "true" : void 0,
		arialabel: ariaLabel,
		revealarialabel: revealAriaLabel,
		mt,
		mr,
		mb,
		ml,
		version: "2",
		children: [
			description && typeof description !== "string" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				slot: "description",
				children: description
			}),
			reveal && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				slot: "reveal",
				children: reveal
			}),
			children
		]
	});
}
function GoabRadioGroup({ disabled, error, onChange, name, children, size = "default", ...rest }) {
	const el = (0, import_react.useRef)(null);
	const _props = transformProps({
		size,
		...rest
	}, lowercase);
	(0, import_react.useEffect)(() => {
		if (!el.current) return;
		const listener = (e) => {
			const detail = e.detail;
			onChange?.({
				...detail,
				event: e
			});
		};
		const currentEl = el.current;
		if (onChange) currentEl.addEventListener("_change", listener);
		return () => {
			if (onChange) currentEl.removeEventListener("_change", listener);
		};
	}, [name, onChange]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-radio-group", {
		ref: el,
		..._props,
		name,
		disabled: disabled ? "true" : void 0,
		error: error ? "true" : void 0,
		version: "2",
		children
	});
}
function GoabSideMenuGroup({ heading, icon, testId, children, mt, mr, mb, ml }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-side-menu-group", {
		heading,
		icon,
		testid: testId,
		mt,
		mr,
		mb,
		ml,
		version: "2",
		children
	});
}
function GoabSideMenuHeading({ meta, testId, icon, children }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("goa-side-menu-heading", {
		icon,
		testid: testId,
		version: "2",
		children: [children, meta && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
			slot: "meta",
			children: meta
		})]
	});
}
function GoabSideMenu({ testId, children }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-side-menu", {
		testid: testId,
		version: "2",
		children
	});
}
var GoabSkeleton = ({ maxWidth, size, lineCount, type, testId, mt, mr, mb, ml }) => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-skeleton", {
		maxwidth: maxWidth,
		linecount: lineCount,
		type,
		size,
		mt,
		mr,
		mb,
		ml,
		testid: testId
	});
};
function GoabSpacer(props) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-spacer", {
		hspacing: props.hSpacing,
		vspacing: props.vSpacing,
		testid: props.testId
	});
}
function GoabSpinner({ type, size, progress, invert, testId }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-spinner", {
		type,
		size,
		progress,
		invert: invert ? "true" : void 0,
		testid: testId
	});
}
function GoabScrollPanel({ header, children, footer, height, testId, ...rest }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("goa-scroll-panel", {
		height,
		testid: testId,
		style: height ? { height } : void 0,
		...rest,
		children: [
			header && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				slot: "header",
				children: header
			}),
			children,
			footer && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				slot: "footer",
				children: footer
			})
		]
	});
}
function GoabTable({ onSort, onMultiSort, sortMode, ...props }) {
	const ref = (0, import_react.useRef)(null);
	(0, import_react.useEffect)(() => {
		if (!ref.current) return;
		const current = ref.current;
		const sortListener = (e) => {
			const detail = e.detail;
			onSort?.(detail);
		};
		const multiSortListener = (e) => {
			const detail = e.detail;
			onMultiSort?.(detail);
		};
		current.addEventListener("_sort", sortListener);
		current.addEventListener("_multisort", multiSortListener);
		return () => {
			current.removeEventListener("_sort", sortListener);
			current.removeEventListener("_multisort", multiSortListener);
		};
	}, [
		ref,
		onSort,
		onMultiSort
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-table", {
		ref,
		width: props.width,
		variant: props.variant,
		"sort-mode": sortMode,
		striped: props.striped ? "true" : void 0,
		testid: props.testId,
		mt: props.mt,
		mb: props.mb,
		ml: props.ml,
		mr: props.mr,
		version: "2",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("table", {
			style: { width: "100%" },
			children: props.children
		})
	});
}
function GoabTableSortHeader({ name, direction = "none", sortOrder, children, ...rest }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-table-sort-header", {
		name,
		direction,
		"sort-order": sortOrder,
		version: "2",
		...rest,
		children
	});
}
function GoabTabs({ initialTab, children, testId, onChange, variant, orientation, navigation }) {
	const ref = (0, import_react.useRef)(null);
	(0, import_react.useEffect)(() => {
		const element = ref.current;
		if (element && onChange) {
			const handler = (event) => {
				const detail = event.detail;
				onChange(detail);
			};
			element.addEventListener("_change", handler);
			return () => {
				element.removeEventListener("_change", handler);
			};
		}
	}, [onChange]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-tabs", {
		ref,
		initialtab: initialTab,
		testid: testId,
		variant,
		version: "2",
		orientation,
		navigation,
		children
	});
}
function GoabTab({ heading, disabled, slug, children }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("goa-tab", {
		slug,
		disabled: disabled ? "true" : void 0,
		heading: typeof heading === "string" ? heading : void 0,
		children: [typeof heading !== "string" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
			slot: "heading",
			children: heading
		}), children]
	});
}
var GoabTemporaryNotificationCtrl = ({ verticalPosition = "bottom", horizontalPosition = "center", testId, ...rest }) => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-temp-notification-ctrl", {
		ref: (0, import_react.useRef)(null),
		...transformProps({
			"vertical-position": verticalPosition,
			"horizontal-position": horizontalPosition,
			...rest
		}, kebab),
		testid: testId
	});
};
function GoabText({ as, tag, children, ...rest }) {
	const _props = transformProps(rest, lowercase);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-text", {
		as: tag || as,
		..._props,
		children
	});
}
function GoabTextArea({ readOnly, disabled, error, onChange, onKeyPress, onBlur, ...rest }) {
	const el = (0, import_react.useRef)(null);
	const _props = transformProps(rest, lowercase);
	(0, import_react.useEffect)(() => {
		if (!el.current) return;
		const current = el.current;
		const changeListener = (e) => {
			const detail = e.detail;
			onChange?.({
				...detail,
				event: e
			});
		};
		const keypressListener = (e) => {
			const detail = e.detail;
			onKeyPress?.({
				...detail,
				event: e
			});
		};
		const blurListener = (e) => {
			const detail = e.detail;
			onBlur?.({
				...detail,
				event: e
			});
		};
		current.addEventListener("_change", changeListener);
		current.addEventListener("_keyPress", keypressListener);
		current.addEventListener("_blur", blurListener);
		return () => {
			current.removeEventListener("_change", changeListener);
			current.removeEventListener("_keyPress", keypressListener);
			current.removeEventListener("_blur", blurListener);
		};
	}, [
		el,
		onChange,
		onKeyPress,
		onBlur
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-textarea", {
		ref: el,
		readOnly: readOnly ? "true" : void 0,
		disabled: disabled ? "true" : void 0,
		error: error ? "true" : void 0,
		version: "2",
		..._props
	});
}
function GoabThreeColumnLayout(props) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("goa-three-column-layout", {
		leftcolumnwidth: props.leftColumnWidth,
		rightcolumnwidth: props.rightColumnWidth,
		maxcontentwidth: props.maxContentWidth,
		children: [
			props.header && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				slot: "header",
				children: props.header
			}),
			props.nav && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				slot: "nav",
				children: props.nav
			}),
			props.sidebar && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				slot: "side-menu",
				children: props.sidebar
			}),
			props.sideMenu && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				slot: "side-menu",
				children: props.sideMenu
			}),
			props.children,
			props.footer && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				slot: "footer",
				children: props.footer
			})
		]
	});
}
function GoabTooltip({ content, children, ...rest }) {
	const _props = transformProps(rest, lowercase);
	const isStringContent = typeof content === "string";
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("goa-tooltip", {
		content: isStringContent ? content : void 0,
		..._props,
		children: [!isStringContent && content && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			slot: "content",
			children: content
		}), children]
	});
}
function GoabTwoColumnLayout(props) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("goa-two-column-layout", {
		navcolumnwidth: props.navColumnWidth,
		maxcontentwidth: props.maxContentWidth,
		children: [
			props.header && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				slot: "header",
				children: props.header
			}),
			props.nav && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				slot: "nav",
				children: props.nav
			}),
			props.children,
			props.footer && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				slot: "footer",
				children: props.footer
			})
		]
	});
}
var GoabFilterChip = ({ iconTheme = "outline", error, onClick, ...rest }) => {
	const el = (0, import_react.useRef)(null);
	const _props = transformProps({
		icontheme: iconTheme,
		...rest
	}, lowercase);
	(0, import_react.useEffect)(() => {
		if (!el.current) return;
		if (!onClick) return;
		const current = el.current;
		current.addEventListener("_click", onClick);
		return () => {
			current.removeEventListener("_click", onClick);
		};
	}, [el, onClick]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-filter-chip", {
		ref: el,
		error: error ? "true" : void 0,
		version: "2",
		..._props
	});
};
function usePublicFormController(type = "details") {
	const controllerRef = (0, import_react.useRef)(new PublicFormController(type));
	const [state, setState] = (0, import_react.useState)(void 0);
	(0, import_react.useEffect)(() => {
		var _a, _b;
		const originalStateGetter = (_a = Object.getOwnPropertyDescriptor(Object.getPrototypeOf(controllerRef.current), "state")) == null ? void 0 : _a.get;
		const originalStateSetter = (_b = Object.getOwnPropertyDescriptor(Object.getPrototypeOf(controllerRef.current), "state")) == null ? void 0 : _b.set;
		if (originalStateGetter && originalStateSetter) Object.defineProperty(controllerRef.current, "state", {
			get: function() {
				return originalStateGetter.call(this);
			},
			set: function(value) {
				originalStateSetter.call(this, value);
				setState(value);
			},
			configurable: true
		});
	}, []);
	return {
		state,
		init: (0, import_react.useCallback)((e) => {
			var _a;
			if (!((_a = e.detail) == null ? void 0 : _a.el)) {
				console.error("El is null during initialization");
				return;
			}
			controllerRef.current.init(e);
		}, []),
		initList: (0, import_react.useCallback)((e) => {
			var _a;
			const customEvent = e;
			if (!((_a = customEvent == null ? void 0 : customEvent.detail) == null ? void 0 : _a.el)) {
				console.error("El is null during list initialization");
				return;
			}
			controllerRef.current.initList(e);
		}, []),
		initState: (0, import_react.useCallback)((state2, callback) => {
			if (!controllerRef.current._formRef) {
				console.error("Form ref not set.");
				return;
			}
			controllerRef.current.initState(state2, callback);
		}, []),
		continueTo: (0, import_react.useCallback)((next) => {
			controllerRef.current.continueTo(next);
		}, []),
		validate: (0, import_react.useCallback)((e, field, validators) => {
			return controllerRef.current.validate(e, field, validators);
		}, []),
		getStateValue: (0, import_react.useCallback)((group, key) => {
			return controllerRef.current.getStateValue(group, key);
		}, []),
		getStateList: (0, import_react.useCallback)(() => {
			return controllerRef.current.getStateList();
		}, []),
		complete: (0, import_react.useCallback)(() => {
			controllerRef.current.complete();
		}, []),
		completeSubform: (0, import_react.useCallback)(() => {
			controllerRef.current.completeSubform();
		}, []),
		controller: controllerRef.current
	};
}
function GoabWorkSideMenu({ heading, url, userName, userSecondaryText, testId, primaryContent, secondaryContent, accountContent, open, onToggle, onNavigate }) {
	const el = (0, import_react.useRef)(null);
	(0, import_react.useEffect)(() => {
		const node = el.current;
		if (!node || !onToggle) return;
		node.addEventListener("_toggle", onToggle);
		return () => {
			node.removeEventListener("_toggle", onToggle);
		};
	}, [onToggle]);
	(0, import_react.useEffect)(() => {
		const node = el.current;
		if (!node || !onNavigate) return;
		const handler = (e) => {
			onNavigate(e.detail.url);
		};
		node.addEventListener("_navigate", handler);
		return () => {
			node.removeEventListener("_navigate", handler);
		};
	}, [onNavigate]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("goa-work-side-menu", {
		ref: el,
		heading,
		url,
		"user-name": userName,
		"user-secondary-text": userSecondaryText,
		open: open ? true : false,
		testid: testId,
		children: [
			primaryContent && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				slot: "primary",
				children: primaryContent
			}),
			secondaryContent && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				slot: "secondary",
				children: secondaryContent
			}),
			accountContent && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				slot: "account",
				children: accountContent
			})
		]
	});
}
function GoabWorkSideMenuGroup(props) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-work-side-menu-group", {
		heading: props.heading,
		icon: props.icon,
		open: props.open ? true : void 0,
		testid: props.testId,
		children: props.children
	});
}
function GoabWorkSideMenuItem(props) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("goa-work-side-menu-item", {
		label: props.label,
		url: props.url,
		badge: props.badge,
		current: props.current ? "true" : void 0,
		divider: props.divider ? "true" : void 0,
		icon: props.icon,
		testid: props.testId,
		type: props.type,
		children: [
			props.popoverContent && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				slot: "popoverContent",
				children: props.popoverContent
			}),
			props.trailingContent && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				slot: "trailingContent",
				children: props.trailingContent
			}),
			props.children
		]
	});
}
function GoabWorkSideNotificationItem({ type, timestamp, title, description, readStatus, priority, testId, onClick }) {
	const el = (0, import_react.useRef)(null);
	(0, import_react.useEffect)(() => {
		if (!el.current) return;
		const handleClick = () => {
			onClick?.();
		};
		el.current.addEventListener("_click", handleClick);
		return () => {
			var _a;
			(_a = el.current) == null || _a.removeEventListener("_click", handleClick);
		};
	}, [el, onClick]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-work-side-notification-item", {
		ref: el,
		type,
		timestamp,
		title,
		description,
		"read-status": readStatus,
		priority,
		testid: testId
	});
}
function GoabWorkSideNotificationPanel({ heading, activeTab, testId, children, onMarkAllRead, onViewAll }) {
	const el = (0, import_react.useRef)(null);
	(0, import_react.useEffect)(() => {
		if (!el.current) return;
		const handleMarkAllRead = () => {
			onMarkAllRead?.();
		};
		const handleViewAll = () => {
			onViewAll?.();
		};
		el.current.addEventListener("_markAllRead", handleMarkAllRead);
		el.current.addEventListener("_viewAll", handleViewAll);
		return () => {
			var _a, _b;
			(_a = el.current) == null || _a.removeEventListener("_markAllRead", handleMarkAllRead);
			(_b = el.current) == null || _b.removeEventListener("_viewAll", handleViewAll);
		};
	}, [
		el,
		onMarkAllRead,
		onViewAll
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("goa-work-side-notification-panel", {
		ref: el,
		heading,
		"active-tab": activeTab,
		testid: testId,
		children
	});
}
var STORAGE_KEY = "goab-theme";
var ATTRIBUTE = "data-theme";
var GoabThemeContext = (0, import_react.createContext)(null);
function readInitialMode(defaultMode) {
	if (typeof window === "undefined") return defaultMode;
	try {
		const stored = window.localStorage.getItem(STORAGE_KEY);
		if (stored === "light" || stored === "dark") return stored;
	} catch {}
	const fromAttribute = document.documentElement.getAttribute(ATTRIBUTE);
	if (fromAttribute === "light" || fromAttribute === "dark") return fromAttribute;
	if (window.matchMedia("(prefers-color-scheme: dark)").matches) return "dark";
	return defaultMode;
}
function GoabThemeProvider({ children, defaultMode = "light" }) {
	const [mode, setModeState] = (0, import_react.useState)(() => readInitialMode(defaultMode));
	(0, import_react.useEffect)(() => {
		if (typeof document === "undefined") return;
		document.documentElement.setAttribute(ATTRIBUTE, mode);
		try {
			window.localStorage.setItem(STORAGE_KEY, mode);
		} catch {}
	}, [mode]);
	const setMode = (0, import_react.useCallback)((next) => {
		setModeState(next);
	}, []);
	const toggle = (0, import_react.useCallback)(() => {
		setModeState((prev) => prev === "light" ? "dark" : "light");
	}, []);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GoabThemeContext.Provider, {
		value: {
			mode,
			setMode,
			toggle
		},
		children
	});
}
function useTheme() {
	const ctx = (0, import_react.useContext)(GoabThemeContext);
	if (!ctx) throw new Error("useTheme must be used within a GoabThemeProvider");
	return ctx;
}
var GoabWorkspaceLayoutScrollStateDefault = {
	scrollPosition: GoabWorkspaceLayoutScrollState.NO_SCROLL,
	isScrollable: false
};
var GoabWorkspaceLayoutScrollStateContext = (0, import_react.createContext)(GoabWorkspaceLayoutScrollStateDefault);
function useGoabWorkspaceLayoutScrollState() {
	return (0, import_react.useContext)(GoabWorkspaceLayoutScrollStateContext);
}
function GoabWorkspaceLayout({ sideMenu, pageHeader, pageFooter, pushDrawer, children, testId, onScrollStateChange, ...rest }) {
	const ref = (0, import_react.useRef)(null);
	const [scrollState, setScrollState] = (0, import_react.useState)(GoabWorkspaceLayoutScrollStateDefault);
	(0, import_react.useEffect)(() => {
		const el = ref.current;
		if (!el) return;
		const listener = (e) => {
			const detail = e.detail;
			setScrollState({
				scrollPosition: detail.state,
				isScrollable: detail.isScrollable
			});
			onScrollStateChange?.({
				...detail,
				event: e
			});
		};
		el.addEventListener("_scrollStateChange", listener);
		return () => el.removeEventListener("_scrollStateChange", listener);
	}, [onScrollStateChange]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GoabWorkspaceLayoutScrollStateContext.Provider, {
		value: scrollState,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("goa-workspace-layout", {
			ref,
			testid: testId,
			...rest,
			children: [
				sideMenu && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					slot: "side-menu",
					children: sideMenu
				}),
				pageHeader && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					slot: "page-header",
					children: pageHeader
				}),
				children,
				pageFooter && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					slot: "page-footer",
					children: pageFooter
				}),
				pushDrawer && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					slot: "push-drawer",
					children: pushDrawer
				})
			]
		})
	});
}
//#endregion
export { GoALinkButton, GoabAccordion, GoabAppFooter, GoabAppFooterMetaSection, GoabAppFooterNavSection, GoabAppHeader, GoabAppHeaderMenu, GoabBadge, GoabBlock, GoabButton, GoabButtonGroup, GoabCalendar, GoabCallout, GoabCheckbox, GoabCheckboxList, GoabChip, GoabCircularProgress, GoabContainer, GoabDataGrid, GoabDatePicker, GoabDetails, GoabDivider, GoabDrawer, GoabDropdown, GoabDropdownItem, GoabDropdownOption, GoabFieldset, GoabFileUploadCard, GoabFileUploadInput, GoabFilterChip, GoabFormItem, GoabFormStep, GoabFormStepper, GoabGrid, GoabHeroBanner, GoabHeroBannerActions, GoabIcon, GoabIconButton, GoabInput, GoabInputDate, GoabInputDateTime, GoabInputEmail, GoabInputFile, GoabInputMonth, GoabInputNumber, GoabInputPassword, GoabInputRange, GoabInputSearch, GoabInputTel, GoabInputText, GoabInputTime, GoabInputUrl, GoabLinearProgress, GoabLink, GoabMenuAction, GoabMenuButton, GoabMicrositeHeader, GoabModal, GoabNotification, GoabOneColumnLayout, GoabPageBlock, GoabPages, GoabPagination, GoabPopover, GoabPublicForm, GoabPublicFormPage, GoabPublicFormSummary, GoabPublicFormTask, GoabPublicFormTaskList, GoabPublicSubform, GoabPublicSubformIndex, GoabPushDrawer, GoabRadioGroup, GoabRadioItem, GoabScrollPanel, GoabSideMenu, GoabSideMenuGroup, GoabSideMenuHeading, GoabSkeleton, GoabSpacer, GoabSpinner, GoabTab, GoabTable, GoabTableSortHeader, GoabTabs, GoabTemporaryNotificationCtrl, GoabText, GoabTextArea, GoabTextArea as GoabTextarea, GoabThemeProvider, GoabThreeColumnLayout, GoabTooltip, GoabTwoColumnLayout, GoabWorkSideMenu, GoabWorkSideMenuGroup, GoabWorkSideMenuItem, GoabWorkSideNotificationItem, GoabWorkSideNotificationPanel, GoabWorkspaceLayout, useGoabWorkspaceLayoutScrollState, usePublicFormController, useTheme };
