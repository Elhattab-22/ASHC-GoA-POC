import { GoabCallout } from "@abgov/react-components";

export default function PotentialHousingCallout() {
  return (
    <GoabCallout heading="Potential housing options" type="information">
      Once you're done, you'll see applicable housing options. Seeing these
      options <strong>does not</strong> mean there are units available right
      now, but you can learn more and get details on how to apply.
    </GoabCallout>
  );
}
